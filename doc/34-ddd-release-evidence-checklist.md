# DDD Release Evidence Checklist

本清单用于把“全�?DDD + 高性能架构升级方案”的完成条件落到可执行证据。发布前�?`DDD_RELEASE_EVIDENCE_STRICT=true node bin/ddd-release-evidence-gate.mjs` 为最终汇总门禁；没有真实环境证据时，只能视为代码级完成，不能视为发布完成�?

## 1. 必须归档�?Artifact

| 证据 | 默认路径 | 生成入口 | strict 规则 |
| --- | --- | --- | --- |
| 后端构建产物 | `artifacts/ddd/build/backend-build-evidence.json` | `DDD_BACKEND_BUILD_STRICT=true node bin/ddd-backend-build-evidence.mjs` | strict 要求后端 package 成功且不能跳过本�?Maven package 执行；共�?backend evidence contract 中的必需 owner/deployable module 均有报告、启动入口存在、jar 产物存在，module 清单不能缺失、重复或包含未知项，`deployable`/`entrypoint` 必须匹配契约，jar metadata 必须包含路径、正数大小和 64 �?SHA-256；`summary.modules/deployableModules/jars/missingEntrypoints/missingClasses/missingJars` 必须�?`modules[]` 明细一致，并带环境、版本和执行�?provenance |
| Docker 镜像构建产物 | `artifacts/ddd/build/docker-image-evidence.json` | `DDD_DOCKER_BUILD_STRICT=true node bin/ddd-docker-build-evidence.mjs` | strict 要求共享 docker evidence contract 中的 `lumira-server` �?`lumira-ui` 两个可部署镜像均构建成功、未跳过并可 inspect；artifact 必须包含环境、版本和执行�?provenance；`summary.images/passed/failed/skipped/blockers` 必须�?`images[]` �?artifact blockers 明细一致，`status` 必须与镜像和 blockers 明细一致；顶层 `blockers[]` 必须�?skipped 镜像�?preflight/provenance blocker �?failed 镜像�?`imageName: blocker` 精确推导；镜像报告必须精确匹�?required image 清单，不能缺失、重复或包含未知镜像，Dockerfile 路径、期望端口、non-root 要求、tag �?Dockerfile SHA-256 必须完整且与静态检查一致；`lumira-server` 必须�?root 运行并暴�?`8080/tcp`，`lumira-ui` 必须暴露 `80/tcp`，镜像必须有 entrypoint �?command；artifact 会在 Docker build 前写�?`staticDockerfile` 静态合规结果，即使 Docker CLI/daemon 不可用也能区�?Dockerfile 契约是否漂移；若 Docker CLI/daemon 不可用导致跳过，每个 skipped image 必须记录具体 `skipReason` �?blockers，且 `skipReason` 必须等于 image blockers 串联结果；PASS 镜像不得残留 blocker；`lumira-server` 默认不下�?OpenTelemetry javaagent，只有设�?`DDD_DOCKER_OTEL_JAVAAGENT_URL` �?compose build arg `OTEL_JAVAAGENT_URL` 才打�?agent，避免默认关闭的观测能力阻塞发布构建；readiness summary 会显�?CLI/daemon 状态、静�?Dockerfile 状态和镜像级恢复动作，但仍不得放行 |
| 后端关键测试 | `artifacts/ddd/tests/backend-test-evidence.json` | `./mvnw test && DDD_BACKEND_TEST_EVIDENCE_STRICT=true node bin/ddd-backend-test-evidence.mjs` | strict 要求共享 backend evidence contract 中的关键架构、领域模型、owner、AI、Payment、File、Job 测试全部存在�?failures/errors �?0；系统级 `DddArchitectureBoundaryTest`、架构目�?controller 测试、插�?`PluginArchitectureContractTest`，以�?IAM/Auth/Platform/Message/File/Plugin/Localization/Payment/AI/Job �?domain model tests 都是必需 DDD 护栏，不能缺失�? tests 或全�?skipped；`summary.suites/tests/failures/errors/skipped/required/requiredPresent/requiredMissing` 必须�?Surefire `suites[]` 明细一致，并带环境、版本和执行�?provenance |
| 热点 SQL EXPLAIN | `tmp/ddd-explain/*.json` | `DDD_EXPLAIN_STRICT=true node bin/ddd-collect-explain.mjs && DDD_EXPLAIN_STRICT=true node bin/ddd-explain-gate.mjs` | strict 采集阶段会先要求 `DDD_EVIDENCE_ENVIRONMENT`、`DDD_RELEASE_CANDIDATE`、`DDD_EVIDENCE_OPERATOR`、`DDD_EXPLAIN_DATABASE` 和非 localhost �?`MYSQL_HOST`，避免本地诊�?EXPLAIN 误入发布证据；strict gate 要求共享 explain evidence contract 中的必需热点 SQL 文件齐全、元数据完整、artifact 自身�?`database` 和非本机 `mysqlHost`、`queryName` 与文件名一致、`sqlSha256` �?64 �?hex 且匹配共享热�?SQL 契约、不得使�?legacy 导入计划、无全表扫描、每�?table plan node �?`table_name/access_type/rows_examined_per_scan` 诊断字段，命中各热点声明索引，并�?`rows_examined_per_scan` �?`rows` 不超�?`doc/28-ddd-hot-path-explain-plan.md` �?strict rows 上限 |
| 前端生产构建 | `artifacts/ddd/lumira-ui/lumira-ui-build-evidence.json` | `DDD_FRONTEND_BUILD_STRICT=true node bin/ddd-frontend-build-evidence.mjs` | strict 要求共享 lumira-ui evidence contract 中的 production build 成功且未跳过、`dist/index.html` 和静态资源存在，并带环境、版本和执行�?provenance；build command/exitCode/durationMs、`summary.files/assets/totalBytes/indexHtmlPresent`、entrypoint bytes/SHA-256、`largestFiles[]` bytes/SHA-256 都必须完整，largestFiles 必须按大小降序且不能重复 |
| 前端静态与单元测试 | `artifacts/ddd/lumira-ui/lumira-ui-static-evidence.json` | `DDD_FRONTEND_STATIC_STRICT=true node bin/ddd-frontend-static-evidence.mjs` | strict 要求共享 lumira-ui evidence contract 中的 lint、typecheck、unit test 均实际运行且通过，`summary.commands/passed/failed/skipped/durationMs` 必须�?`results[]` 明细一致，`status` 必须与命令结果一致，results 不能缺失、重复或包含未知命令，每项必须带 command、exitCode 和非�?durationMs，并带环境、版本和执行�?provenance |
| Flyway 迁移 | `artifacts/ddd/migration/migration-evidence.json`；预检 handoff `artifacts/ddd/migration/migration-evidence-handoff.md` | 先运�?`DDD_MIGRATION_CHECK_ENV=true node bin/ddd-migration-evidence.mjs` 生成 handoff，再用真实证据运�?`DDD_MIGRATION_STRICT=true DDD_RELEASE_CANDIDATE=<sha-or-version> node bin/ddd-migration-evidence.mjs` | strict 要求无重复版本、无�?migration 文件，且新库迁移和旧库升级均有运行证据；预检模式只写 handoff，不写正�?`migration-evidence.json`，用于在采集前列�?sql/release-infra/release-owner 必填变量；artifact 顶层 `generatedAt` 必须�?ISO 时间戳，防止手工拼接或旧格式迁移证据绕过 manifest 时间校验；`summary.locations/migrationFiles/duplicateVersionLocations/emptyFiles/runtimeReady` 必须�?`locations[]` �?`runtime` 明细一致；`locations[]` 必须精确覆盖共享契约中的必需 owner Flyway location，不能缺失、重复或包含未知 location；每�?migration 必须包含 `version`、`description`、`file`、正�?`bytes` �?64 �?SHA-256，且 `migrationCount` 必须匹配 `migrations[]` 数量；生成脚本和 release gate 都会要求运行证据包含环境、release candidate、执行人、ISO 完成时间、fresh DB Flyway/schema-history 证据�?old DB upgrade Flyway/schema-history 证据；两类证据必须是不同的可追溯链接、artifact/log 路径、对�?URI 或工单号，并拒绝占位文本；artifact �?`runtimeReady`、`runtimeProofs` �?`runtimeDiagnostics` 会按 proof/owner/envKeys 展示缺口 |
| 发布 env 文件 lint | `artifacts/ddd/release/release-env-lint.json` | `DDD_RELEASE_ENV_FILE=.env.release node bin/ddd-release-env-file-lint.mjs` | strict manifest 要求�?artifact 存在且带环境、版本和执行�?provenance；lint 会读�?`DDD_RELEASE_ENV_FILE`，拒绝缺失文件、权限过宽、非 safe dotenv 行、重�?key、`__REQUIRED__`、`replace-with-*`、`TODO/TBD`、`example.com/internal/test` 等占位值、空值、localhost、非 HTTPS URL、过�?secret/token �?release config contract 中定义的不合格值；真实 `.env.release` 必须收紧�?`chmod 600`，且只允�?`KEY=value`、`export KEY=value` 或注�?空行，避�?secret �?group/other 读取或在后续执行入口出现 shell 语义；未�?env 文件时会进入 `inputKind=process-environment-only`，只用于本地诊断当前进程变量缺口，不能替代真�?`.env.release`；报告只记录 key、blockers、summary、`inputKind`、`envFileSecurity` �?provenance，不写出明文 secret，其�?`envFileSecurity` 会记录是否检查权限、mode、是否满�?`chmod 600`、缺文件/进程环境 reason，以及模板文件是否跳过真实权限阻断；provenance 优先来自显式 `DDD_EVIDENCE_ENVIRONMENT`、`DDD_RELEASE_CANDIDATE`、`DDD_EVIDENCE_OPERATOR`，本地缺省时会推�?`local-dev`、当�?git commit 和当前用户；如果输入是自动生成的 `release-env-missing.template.env`，artifact 会标�?`inputKind=generated-missing-template`，strict gate 只按“缺真实已填�?DDD_RELEASE_ENV_FILE”这个根因阻断；orchestrator 会在 `release-config-evidence` 之前执行该步骤，防止占位 `.env` 进入后续重型证据流程 |
| 发布环境配置矩阵 | `artifacts/ddd/config/release-config-evidence.json` | 参�?`doc/36-ddd-release-env-template.env`，执�?`DDD_RELEASE_ENV_FILE=.env.release DDD_RELEASE_CONFIG_STRICT=true DDD_EVIDENCE_ENVIRONMENT=staging DDD_RELEASE_CANDIDATE=<sha-or-version> DDD_EVIDENCE_OPERATOR=<operator> node bin/ddd-release-config-evidence.mjs` | strict 要求准生�?生产等价 DB、Redis host/port、owner URL、Job token、AI provider、AI owner gateway、前后端 baseURL 配置存在且非 localhost；公开/owner/service URL 必须 HTTPS，Redis port 必须是数字端口，关键 secret/token 至少 32 字符，数据库/Redis 密码至少 16 字符；配置值会复用统一 evidence 规则拒绝占位文本；artifact 必须包含 `sourceEnvironment`、`releaseCandidate`、`evidenceOperator`；`summary`、`blockerDetails`、`blockersByGroup`、`blockersByOwner` 必须�?`groups[]`、`coverageMatrix`、`blockers[]` �?`warnings[]` 明细一致；`blockerDetails[]` 必须逐项对应 `blockers[]`，并在指向已知配�?check 时与 `coverageMatrix` �?owner、envKeys、required 保持一致；`coverageMatrix` 必须对每个配置要求精确出现一次，不能缺失、重复或包含未知 check |
| owner readiness/health/metrics | `artifacts/ddd/readiness/summary.json` | `node bin/ddd-runtime-readiness-smoke.mjs` | 共享 runtime readiness contract 要求 `IAM`、`Auth`、`Platform`、`Message`、`File`、`Plugin`、`Localization`、`Payment`、`AI`、`Job` 10 个上下文均有 readiness/health/metrics 三类 endpoint，共 30 个检查，失败数为 0，准生产/生产不能只用 localhost�?0 项必须精确覆盖，未知 context/suffix、重复检查或重复 artifact 引用都会阻断；contract 会读取每�?endpoint artifact，校�?readiness �?owner/API/health/metrics/dependencies/rollback payload、health �?healthChecks/metrics payload、metrics �?metrics payload 均非空且字段完整；strict 运行必须带结构化 `productionEquivalence`，生成阶段会拒绝�?HTTPS、localhost baseURL 或缺�?`DDD_RUNTIME_READINESS_DEPLOYMENT_EVIDENCE`/`DDD_DEPLOYMENT_EVIDENCE` 部署证据引用，历�?artifact 缺少该字段会�?strict contract 阻断；readiness summary 会显�?baseURL/localOnly、production-equivalence issues�?0 项覆盖和逐上下文 ready/missing/non-200 诊断 |
| 认证热路径性能 | `artifacts/ddd/performance/authenticated-runtime-actual.json` | `node bin/ddd-authenticated-performance-smoke.mjs` | 失败数为 0，p95 有值，上传回包 200；`checkedAt`、duration、concurrency、整�?p50/p95/p99、`ok + failed`、�?samples、�?endpoint samples、statusCounts、keepalive one-shot �?endpoint 覆盖必须自洽；endpoint 清单必须精确覆盖 current-user、tenant、permissions、message unread/list、files list、plugin bootstrap、localization runtime、payment providers 9 个热路径，不能缺失或出现未知 endpoint；上传证据必须是 `/api/v2/files/upload` 且带 fileId；strict 运行必须带结构化 `productionEquivalence`，要求后�?baseURL �?HTTPS 且非 localhost，并必须�?`DDD_AUTH_PERF_DEPLOYMENT_EVIDENCE` �?`DDD_DEPLOYMENT_EVIDENCE` 附加部署证据引用；缺失该字段的历�?actual 会被 strict gate 作为 authenticated-performance-shape blocker 阻断；readiness summary 会显�?actual �?baseURL/localOnly、production-equivalence issues、失败数、p95、上传回包耗时�?endpoint 覆盖 |
| 认证热路径性能 baseline | `artifacts/ddd/performance/authenticated-runtime-baseline.json`；晋级审�?`artifacts/ddd/performance/authenticated-runtime-baseline-promotion.json` | `DDD_AUTH_PERF_BASELINE_ACCEPTED_BY=... DDD_AUTH_PERF_BASELINE_ENVIRONMENT=... node bin/ddd-promote-performance-baseline.mjs` | strict 要求 baseline 来自 HTTPS、非 localhost、带结构�?`productionEquivalence.strict=true` 的生产等�?actual，source actual 自身必须带完�?provenance，actual `sourceEnvironment` 必须匹配晋级环境，actual `releaseCandidate` 必须匹配 `DDD_RELEASE_CANDIDATE`；baseline 必须�?`baselineType=authenticated-runtime`、ISO `acceptedAt`、非占位 `acceptedBy`、`sourceEnvironment`、`sourceArtifact`、`sourceSha256`、`releaseCandidate`、`evidenceOperator`；`sourceSha256` 必须�?64 �?SHA-256，晋级审计也必须记录同一源文�?hash；baseline �?actual 都必须包含整�?p95、上传回包耗时、�?endpoint `samples` �?`p95`，且 `perEndpoint` 必须无条件覆盖固�?9 个热路径，不能靠省略 `endpoints[]` 少测端点；还必须通过 samples/statusCounts/oneShots/endpoints、分位数顺序和上传证据自洽校验；actual 必须覆盖 baseline 中所�?endpoint；整�?p95、�?endpoint p95 和上传回包耗时不超�?baseline 10%；晋级脚本成�?失败都会�?promotion audit，readiness summary 会显�?baseline 缺失时的晋级 envKeys、最近一�?promotion blockers、metadata/shape 问题和回归项 |
| File 异步处理 E2E | `artifacts/ddd/file/file-processing-e2e.json` | `node bin/ddd-file-processing-e2e-smoke.mjs` | 共享 business E2E evidence contract 要求 `SECURITY_SCAN`、`TEXT_EXTRACT`、`AI_PARSE` 均成功，并产�?`SECURITY_SCAN_RESULT`、`TEXT_CONTENT`、`AI_PARSE_READY` artifact；任务和产物类型不得重复或未知，任务必须�?created/claimed/completed 时间、retryCount=0 且无 lastError，产物必须绑定正�?task、带 created/updated 时间和正�?`contentLength`；上传必须返�?txt/text/plain 文件元数据和正数耗时，job run 必须实际处理任务，处�?backlog、failed backlog、dead-letter 不能恶化；strict 运行会写�?`productionEquivalence`，要求后�?baseURL �?HTTPS 且非 localhost，可�?`DDD_FILE_PROCESSING_DEPLOYMENT_EVIDENCE` �?`DDD_BUSINESS_E2E_DEPLOYMENT_EVIDENCE` 附加部署证据引用；strict contract 会要�?artifact 自身携带结构�?`productionEquivalence`；readiness summary 会显示任�?产物逐项状态、localOnly �?production-equivalence issues |
| Payment webhook E2E | `artifacts/ddd/payment/payment-webhook-e2e.json` | `node bin/ddd-payment-webhook-e2e-smoke.mjs` | 共享 business E2E evidence contract 要求有效签名推进订单�?`PAID`，重�?event 幂等返回已处理事件，nonce replay 和坏签名被拒绝且数据库行记录正确拒绝原因；provider 必须 enabled/configured、包�?webhookSecret 配置字段并有正数耗时，创建订单必须处�?`PENDING` 且最终订单号、金额、币种一致；四类 webhook 场景必须�?eventId、`payment.succeeded` eventType、预�?processMessage 和正数耗时，webhook event 行不能重复，valid 行必�?processed/signatureValid 且有 processedAt，四�?eventId 必须符合幂等契约；strict 运行会写�?`productionEquivalence`，要�?webhook baseURL �?HTTPS 且非 localhost，可�?`DDD_PAYMENT_WEBHOOK_DEPLOYMENT_EVIDENCE` �?`DDD_BUSINESS_E2E_DEPLOYMENT_EVIDENCE` 附加 provider/sandbox 部署证据引用；strict contract 会要�?artifact 自身携带结构�?`productionEquivalence`；readiness summary 会显示四�?webhook 场景、localOnly �?production-equivalence issues |
| Outbox replay/dead-letter | `artifacts/ddd/outbox/outbox-replay-dead-letter-test-evidence.json` | `DDD_OUTBOX_SMOKE_STRICT=true node bin/ddd-outbox-replay-dead-letter-smoke.mjs` | 共享 Outbox/Job evidence contract 要求 Message/System/File/Payment/Plugin owner relay 测试报告均存在且无失败、无错误、无 skipped；报�?className 不得重复，每�?report 必须存在�?tests 为正数、timeSeconds 非负、reportPath 完整；`testedContracts[]` 必须精确覆盖 claim 防重、delivered、失败重试、dead-letter、manual replay、relay disabled replay 六类行为，不能缺失、重复或未知；strict 要求 artifact 带环境、版本和执行�?provenance |
| Job owner relay E2E | `artifacts/ddd/jobs/job-e2e-smoke.json` | `node bin/ddd-job-e2e-smoke.mjs` | 共享 Outbox/Job evidence contract 要求未授权请求被拒绝，未授权探测路径固定�?`/internal/jobs/outbox/relay`�? �?owner/internal job endpoint 精确覆盖且全部成功、返回类型正确，�?owner payload 失败不增长；endpoint name 不得重复，不能缺失或出现未知 endpoint，`baseUrl`、ISO `checkedAt`、`summary.total/failed/maxElapsedMs` 必须�?endpoint 明细一致，每个 endpoint 必须有正数耗时；strict 运行会写�?`productionEquivalence`，要�?job/internal baseURL �?HTTPS 且非 localhost，可�?`DDD_JOB_SMOKE_DEPLOYMENT_EVIDENCE` �?`DDD_BUSINESS_E2E_DEPLOYMENT_EVIDENCE` 附加部署证据引用；strict contract 会要�?artifact 自身携带结构�?`productionEquivalence`；readiness summary 会�?endpoint 显示状态、返回类型、outbox ownership delta �?production-equivalence issues |
| AI runtime drill | `artifacts/ddd/ai/ai-runtime-drill.json` | `node bin/ddd-ai-runtime-drill.mjs` | strict 要求远程 provider 和远�?owner gateway 证据，且 `LUMIRA_AI_BASE_URL`/`LUMIRA_BASE_URL` 必须�?HTTPS、非 localhost 的生产等价地址；artifact 必须写入结构�?`productionEquivalence`，并必须�?`DDD_AI_RUNTIME_DEPLOYMENT_EVIDENCE` �?`DDD_DEPLOYMENT_EVIDENCE` 附加部署证据引用；共�?AI runtime contract 会复�?`baseUrl`、ISO `checkedAt`、`status`、`summary.failed`、`summary.failureCategories`、endpoint 状态和 payload、provider local fallback 标记、owner gateway configured owner 计数与明细一致，以及 strict artifact 自身携带结构�?`productionEquivalence`；PASS artifact 必须 readiness/health/metrics 全部 200，且包含 `/api/v2/ai/chat`、provider/owner gateway health check、provider/owner gateway metrics；`remoteEvidence.provider.remoteConfigured=true` �?provider 不能�?`lumira-local`，owner gateway 必须 `UP` 且至少有一个非重复 configured owner；失败时 `failures[]` 必须�?`failureDetails[].message` 一一对应，并检�?`failureDetails` �?`summary.failureCategories` 分派 endpoint/contract/health/metrics/provider/owner-gateway/provenance 修复 |
| 物理拆分结构门禁 | `artifacts/ddd/split/physical-split-readiness.json` | `DDD_SPLIT_STRICT=true node bin/ddd-physical-split-gate.mjs` | strict release 必须使用 `DDD_SPLIT_STRICT=true` 生成�?artifact；advisory mode 只能用于本地诊断，不能进入严格上线。artifact �?blockers/failures 必须�?0，并带环境、版本和执行�?provenance；共�?physical split contract 会复�?`summary.contexts/physicalServiceTargets/standaloneTargetsReady/globalChecks/contextChecks/passedChecks/failedChecks/failures/blockers/warnings/migrationFiles/missingBusinessEndpoints/crossServiceDependencyFailures` �?contexts、globalChecks �?provenance 明细一致；IAM/Auth/Platform/Message/File/Plugin/Localization/Payment/AI/Job 必须精确覆盖且不能重复或未知，module/route/ownerContext/physicalServiceTarget 必须匹配契约，每个上下文至少包含 module、owner-manifest、readiness、health、metrics、cross-service-pom-dependency 六类基础检�?|
| 前端浏览�?smoke | `artifacts/ddd/lumira-ui/lumira-ui-smoke.json` | `node bin/ddd-frontend-playwright-smoke.mjs && node bin/ddd-frontend-smoke-evidence.mjs` | strict 要求 `DDD_FRONTEND_EXPECT_DEPLOYED=true`、HTTPS 部署 baseURL、运行时 provenance，且共享 lumira-ui smoke contract 定义的核�?smoke flow 全部通过；artifact 必须写入结构�?`productionEquivalence`，并必须�?`DDD_FRONTEND_DEPLOYMENT_EVIDENCE` �?`DDD_DEPLOYMENT_EVIDENCE` 附加部署证据引用；生成脚本和 release gate 会使用同一�?required-flow 校验；`summary.requiredFlows/missingRequiredFlows` 必须�?required/coverage 明细一致，required flow、`flowCoverage` �?`diagnostics.staticSpecCoverage` 不能重复、缺失或出现未知项，passed flow 必须记录匹配到的 Playwright 标题；Playwright 报告存在�?`summary.total/passed/failed/skipped` 必须�?`tests[]` 一致，`status` 必须�?blockers 明细一致；release gate 会按当前 strict/advisory 口径重新计算 `blockers[]` 并逐项比对，防止旧 artifact、缺少结构化 `productionEquivalence` �?JSON 或手�?blocker 列表掩盖缺失 flow、baseURL、部署期望或 provenance；readiness summary 会显�?baseURL/HTTPS/localOnly、production-equivalence issues、expectDeployed、测试总数、缺�?flow、静�?spec 覆盖和修复动�?|
| 回滚演练 | `artifacts/ddd/rollback/rollback-drill.json` | `node bin/ddd-init-rollback-drill.mjs` 初始化草稿，填写真正演练结果后运�?`DDD_ROLLBACK_DRILL_STRICT=true node bin/ddd-rollback-drill-evidence.mjs`；无法安全演练时先用 `node bin/ddd-rollback-deferral-template.mjs` 生成待审�?deferral 输入 | strict 要求 artifact 总状态为 `PASS`；十个上下文均为 `PASS`，或 `DEFERRED` 且有原因、风险接受人和未过期时间；PASS �?`drillEvidence` 必须包含可追溯的链接、artifact/log 路径、对�?URI 或工单号；文本字段复用统一 evidence 规则，`TODO` �?`replace-with-*` 占位文本会被拒绝；deferral 模板不会自动放行，必须填入真实审批证据后�?`DDD_ROLLBACK_DRILL_DEFERRAL_FILE` 合并；每次运�?evidence 脚本都会刷新 `generatedAt` �?`checkedAt`，freshness 只能通过重跑真实证据生成入口恢复，不能手工改 JSON；`summary` 必须�?contexts、contextDiagnostics、blockers、warnings �?appliedDeferrals 明细一致；release gate 会按 rollback 核心合同重新计算 `blockers[]` 并逐项比对；`contextDiagnostics[]` 必须精确覆盖十个上下文，owner/action 匹配共享 remediation，status/ready/missingEvidence/deferralApplied/evidence �?context 明细一�?|
| 热点 SQL explain | `tmp/ddd-explain/*.json` �?`DDD_EXPLAIN_DIR` | `DDD_EXPLAIN_STRICT=true node bin/ddd-collect-explain.mjs && DDD_EXPLAIN_STRICT=true node bin/ddd-explain-gate.mjs` | strict 要求生产量级或准生产量级 EXPLAIN artifact；采集脚本会拒绝缺少 provenance、缺�?`DDD_EXPLAIN_DATABASE` 或使�?localhost/127.0.0.1/::1 �?`MYSQL_HOST`；每�?artifact 必须包含 `generatedAt`、`sourceEnvironment`、`releaseCandidate`、`evidenceOperator`、`queryName`、`sqlSha256`、`database`、`mysqlHost`，其�?`sourceEnvironment` �?`mysqlHost` 不能是本地诊断值，`queryName` 必须匹配文件名、`sqlSha256` 必须�?64 �?hex 且匹配共享热�?SQL 契约，且不得�?legacy 导入计划、全表扫描、缺�?table plan 诊断字段、缺失热点索引或超过 strict rows 上限�?`rows_examined_per_scan`；必需文件�?`platform-runtime-appearance.json`、`plugin-bootstrap.json`、`message-visible-list.json`、`message-unread-count.json`、`message-archive-total.json`、`ai-knowledge-index-retry.json`、`platform-outbox-owner-relay-message.json`、`platform-outbox-owner-relay-file.json`，索引至少覆�?`uk_sys_config_key`、`uk_sys_plugin_tenant_rel`、`uk_sys_plugin_definition_code`、`uk_sys_plugin_version_code_version`、`idx_msg_notice_visible_recent`、`idx_ai_knowledge_document_index_retry` �?`idx_platform_event_outbox_owner_queue` |
| 发布证据 checksum manifest | `artifacts/ddd/release/evidence-manifest.json` | `DDD_RELEASE_MANIFEST_STRICT=true node bin/ddd-release-evidence-manifest.mjs` | strict 要求共享 manifest contract 中的所有关�?JSON artifact 存在、可解析，并记录正数大小�?4 �?hex SHA-256 和可解析时间戳，�?artifact `relativePath` 不重复、不出现未知 artifact report；manifest 自身必须带环境、版本和执行�?provenance，并统计关键 artifact �?provenance 缺失，且会拒�?`local-dev`、`local-worktree`、`local-operator` 等本地诊断值，防止本地证据被误当成生产等价发布证据；`summary.requiredArtifacts/presentArtifacts/optionalArtifacts/invalidJsonArtifacts/provenanceIssueArtifacts/explainFiles/blockers` 必须�?artifact、EXPLAIN �?blockers 明细一致，`status` 必须�?blockers 明细一致；`blockers[]` 会按缺失 artifact、无�?JSON、strict provenance、可选审�?artifact contract issue �?EXPLAIN 状态重新计算并逐项比对；`DDD_RELEASE_MANIFEST_CHECK_ENV=true` 会只执行 manifest provenance/artifact/EXPLAIN 预检并写�?`artifacts/ddd/release/evidence-manifest-preflight.json`，该报告只记�?key 是否存在、artifact 摘要、blockers 和脱�?next action，不�?secret；`artifacts/ddd/release/release-final-owner-queue-run-report.json` 存在时会作为 optional checksum artifact 纳入 manifest 并校�?run report contract，`artifacts/ddd/release/release-execution-run-report.json` 存在时会作为 optional checksum artifact 纳入 manifest 并校�?execution run report contract，`artifacts/ddd/release/release-next-action-run-report.json` 存在时会作为 optional checksum artifact 纳入 manifest 并校�?`ddd-release-next-action-run-report-contract.mjs`，可�?`ddd-release-next-action-run-report-summary.mjs` 输出 Markdown 摘要，`artifacts/ddd/release/explain-gate-report.json` 存在时会作为 optional checksum artifact 纳入 manifest 并校�?EXPLAIN gate report contract，`artifacts/ddd/release/release-unblock-brief.json` 存在时会作为 optional checksum artifact 纳入 manifest 并校�?unblock brief contract，包�?owner env handoff、`Performance Baseline`、前 5 �?`RUN_NOW` `Next Action Queue`、Markdown section 和脱敏路�?命令；`artifacts/ddd/release/release-artifact-path-leak-contract.json` 存在时会作为 optional checksum artifact 纳入 manifest，并要求 `PASS` �?`leakCount=0`；`artifacts/ddd/release/release-env-owner-handoff-redacted-contract.json` 存在时也会作�?optional checksum artifact 纳入 manifest，并要求 `PASS`、`redacted=true`、`issueCount=0`、owner markdown 文件清单一致；`artifacts/ddd/release/release-config-owner-input-reconciliation.json` 存在时会作为 optional checksum artifact 纳入 manifest，并要求 `PASS`、`redacted=true`、`issueCount=0`、`mappedConfigPlaceholderKeys=uniqueConfigPlaceholderKeys` �?`unmappedConfigPlaceholderKeys=0`；`artifacts/ddd/release/release-owner-input-receipt.json` 存在时会作为 optional checksum artifact 纳入 manifest，并要求 `PASS` �?`PENDING_OWNER_INPUT`、`redacted=true`、`envFile=<release-env-file>`、owner 汇总和 pass criteria 自洽；`artifacts/ddd/release/release-owner-input-receipt.csv`、`release-owner-input-receipt-items.csv` �?`release-owner-input-receipt-items.md` 存在时会作为 optional checksum artifact 纳入 manifest，按文本 checksum 记录，不�?JSON contract 解析；`artifacts/ddd/release/evidence-manifest-preflight.json` 是易变的 manifest provenance 预检输出，会�?path-leak 扫描�?preflight 直接消费，但不纳入正�?manifest checksum，避免预检重写导致 checksum 循环漂移；这�?optional artifact 不存在不阻断；GitHub release evidence workflow 会在 preflight capture 后、上�?artifacts 前再次运�?manifest、readiness summary �?unblock brief 刷新，确保非易变 contract report 进入最�?checksum；release config evidence 也会纳入 provenance-sensitive artifact 集合；manifest 不包�?release gate 自身报告，避免循环依�?|
| 发布证据编排报告 | `artifacts/ddd/release/orchestrator-report.json` | `node bin/ddd-release-evidence-orchestrator.mjs --run --strict` | 不替代任何证据；用于按顺序执行并归档每个 evidence 脚本的退出码、耗时、输出尾部和 strict env keys；`--strict` 会向所有子步骤传�?`DDD_RELEASE_EVIDENCE_STRICT=true`，并�?Outbox 状态机 smoke 传�?`DDD_OUTBOX_SMOKE_STRICT=true`、向 physical split gate 传�?`DDD_SPLIT_STRICT=true`；run 模式会先执行 release env 文件 lint，再生成配置矩阵证据，随后运�?Playwright smoke 并转换为统一前端 smoke evidence，并�?manifest 生成前执�?`DDD_RELEASE_MANIFEST_CHECK_ENV=true` provenance preflight；preflight 会先检�?provenance、env file、后�?AI/前端 HTTPS �?localhost baseURL、前端部署期望、Docker CLI/daemon、AI remote 期望和迁移演�?env；strict gate 会要求该报告存在、为 run 模式、带 provenance、包�?26 个预期步骤、每�?selected step 都有同序执行结果，以�?Outbox strict env、physical split strict env �?manifest provenance preflight env；preflight 11 项、selected steps �?executed results 都不能缺失、重复或未知，preflight 必须有合法状态、detail �?envKeys，step 必须�?label/command/envKeys，result �?status/skipped 类型必须正确；preflight 中的每个 `BLOCKER` 也会提升�?strict release gate �?blocker；readiness summary 会显�?plan/run、selected/executed step 数、未执行步骤和恢复动�?|
| 发布行动摘要 | `artifacts/ddd/release/readiness-summary.md` | `node bin/ddd-release-readiness-summary.mjs` | 不作为放行证据；用于把当�?release gate blocker 归类�?owner 和下一步命令，并显式显�?gate mode（strict/advisory），同时展开 runtime、配置、性能、业�?E2E、Docker、前端、迁移、EXPLAIN、AI、回滚和 orchestrator 诊断；summary �?owner/source/blocker-map(JSON/CSV/Markdown)/priority/batch/dependency-graph/execution-queue/missing-env 衍生工件都会写入 `inputArtifacts`，记录所读取 gate、manifest、配置、迁移、Docker、runtime smoke 等输入文件的路径、mtime、generatedAt �?blocker/warning 计数，审计时应确认这�?rollup 引用同一�?`release-evidence-gate.json`；blocker-map CSV 一行一�?owner，适合导入工单或飞书表格，测试会校�?owner 行数�?blockerCount �?JSON 完全一致；会显�?release config `configContractIssue`，且 config contract issue 非空时摘要保�?`NOT_READY`；`diagnostics.readinessSummary.contractIssues` 会复核摘�?`status`、gate blocker 计数、行动项�?owner/category 分组自洽，也会校�?blocker map �?`totalBlockers` �?category 明细覆盖全部 raw blockers，owner rollup �?`pendingItems/items`、`collapsedItems/collapsed` 自洽，release action priority 每项都有 `reason/action` 且不能重复，action batches 必须逐项覆盖 priority 清单、每批都有命令提示，release action dependency graph �?nodes/edges/ready/blocked 集合必须�?batches �?`dependsOn` �?`canRunImmediately` 一致，`graphDensity` 必须等于 `edgeCount / maxDirectedEdges`，`compressedEdges` 必须等于完整边按 priority 聚合后的结果，release execution queue �?ready/blocked 数、`nextBatchIds` 和每�?blocked batch �?`unmetDependencyCount` 必须�?dependency graph �?batches 一致，orchestrator 行动必须是最后的 P3 复核批次，并反解�?`release-env-missing.template.env` 校验它与 `release-env-missing.json` �?`templateEnvKeys` 完全一致、无重复、无额外 key；EXPLAIN 诊断会列出文件数、缺失必需文件、legacy 导入文件�?contract issue，并�?`reasons[]` 汇总到 release action priority �?`reason` 字段，避免数据库行动项缺少可执行原因；release action batches 会保�?action 文本中的命令顺序，确�?env lint 先于 config evidence，且 Docker、业�?E2E、迁移、EXPLAIN、回滚等批次都有可执行命令提示和对应 env key 清单；rollback 批次会提�?`DDD_ROLLBACK_DRILL_FILE`、`DDD_ROLLBACK_DRILL_DEFERRAL_FILE` 等审批输入，EXPLAIN 批次会提�?`DDD_EXPLAIN_DIR` �?MySQL 连接变量；strict orchestrator run-mode 被排�?P3 最终复核批次，只有�?P0/P1/P2 证据修复后再重跑，避免过早失败；owner/source/action-priority rollup 会按根因折叠重复行动项，`collapsedItems` 仅表示“已由更具体的待办覆盖”，不会减少 release gate blocker，也不会隐藏原始诊断；owner rollup artifact 会区�?`activeOwners` �?`collapsedOnlyOwners`，避免只被折叠的 owner 进入待办批次排序。若 env lint 输入不是真实 `release-env-file`（缺�?env file、process environment only �?generated missing template），release-config 的逐项缺失会先折叠�?release env 根因，优先级列表先要求发布负责人提供真实 `DDD_RELEASE_ENV_FILE`，同�?release-config 诊断仍保留完整缺�?key 清单 |
| 发布快速解阻单�?| `artifacts/ddd/release/release-unblock-brief.json`、`.md` | `node bin/ddd-release-unblock-brief.mjs && node bin/ddd-release-unblock-brief-contract.mjs` | 不作为放行证据；用于�?final go/no-go、release env owner handoff、owner input receipt、authenticated performance baseline closure、cutover blocked items、execution waves 和前 5 �?`RUN_NOW` next action 合成发布负责人当前最短路径。contract 必须校验 owner env handoff、owner input receipt �?`PASS/PENDING_OWNER_INPUT` 状态、pending owner 表、missing criteria、`Performance Baseline`、`Next Action Queue`、`Execution waves`、`Wave operator commands`、Markdown section、命令脱敏和路径脱敏；`cutoverAllowed=true` �?owner input receipt 必须�?`PASS` �?`cutoverReady=true`。JSON/Markdown 不得包含 secret、真�?env 文件值、本机仓库绝对路径或用户 home 路径。`ddd-release-env-owner-handoff-redacted-contract.mjs` 会额外校�?redacted handoff JSON/CSV/Markdown 和�?owner 目录文件完全一致，不允�?stale owner 文件、路径穿越、具�?env 赋值、占位符、DSN �?token 进入分派包；preflight 会在 env readiness 前执行该 contract，避免发布负责人按旧 handoff 分派。P0 可运行命令必须来�?`release-execution-commands.sh`，支�?list、env-check、dry-run �?execute；P1/P2/P3 若存�?unmet dependencies，只能显�?blocked-until 依赖，不能给出误导�?execute 命令。brief 只能指导先执行哪�?owner/wave；执行后仍必须刷�?expected artifacts、strict release gate、readiness summary �?final go/no-go gate |

Docker 镜像构建产物也支持显式的既有镜像 inspect 证据路径：设�?`DDD_DOCKER_EXISTING_IMAGE_BUILD_EVIDENCE=<ci-build-artifact-or-run-url>`、`DDD_DOCKER_EXISTING_LUMIRA_SERVER_IMAGE=<registry>/lumira-server:<release-candidate>` �?`DDD_DOCKER_EXISTING_FRONTEND_IMAGE=<registry>/lumira-ui:<release-candidate>` 后，脚本不重�?build，而是对指定镜像执�?`docker image inspect`，并继续校验 Docker CLI/daemon、当�?Dockerfile 静态合同、端口、non-root、entrypoint/cmd、size、required image 清单�?provenance。strict existing-image 模式缺少可信 CI 构建日志、制品清单或镜像 provenance 引用会直接失败；任何 inspect 或合同失败仍会进�?strict blocker，不能作�?Docker 证据 waiver�?

`bin/ddd-release-config-template.test.mjs` 会用 `doc/36-ddd-release-env-template.env` 真实运行 config evidence，确保模板覆盖所有必�?key，但会因占位值被 strict 拒绝。`doc/36-ddd-release-env-template.env` �?`artifacts/ddd/release/release-env-missing.template.env` 都只能作�?secret 填写清单，不能作为发布证据直接提交；后者被 lint 时会�?`inputKind=generated-missing-template` 进入 readiness summary，提示发布负责人先提供真�?`.env.release`。`release-env-missing.json` 会同时输�?`templateEnvKeys/templateEnvKeyCount/templateControlKeys`，其�?`DDD_RELEASE_ENV_FILE` 是控制变量，只出现在说明注释中，不会生成 `__REQUIRED__` 行；每个 group 还会携带对应 batch �?`batchId`、`dependsOn`、`canRunImmediately`、`commands`、`expectedArtifacts` �?`exitCriteria`，并同步写入 `.template.env` 注释，方便直接转发布工单或交�?secret 填写人执行�?

发布 env lint �?release config evidence 会同时输�?`primaryBlockers` �?`placeholderDerivedConfigBlockers`。执行顺序必须先处理 `primaryBlockers`：例如缺少真�?`DDD_RELEASE_ENV_FILE`、`__REQUIRED__`、`replace-with-*`、过�?secret、localhost 或非 HTTPS URL。由这些占位值触发的配置矩阵失败会进�?`placeholderDerivedConfigBlockers`，用于审计完整影响面，但不应拆成几十个独�?owner 待办；替换为真实准生�?生产值后再重新运�?lint �?config evidence，剩余的 `releaseConfigBlockersAfterPlaceholders` 才是需要逐项分派�?owner 的真实配置缺口�?

Orchestrator preflight 是固定发布契约的一部分，当前必须包�?11 项：`release-provenance`、`release-config-env-file`、`backend-runtime-base-url`、`ai-runtime-base-url`、`lumira-ui-runtime-base-url`、`lumira-ui-deployed-expectation`、`docker-cli`、`docker-daemon`、`ai-provider-remote-expectation`、`ai-owner-gateway-remote-expectation`、`migration-runtime-evidence`。共�?orchestrator contract 会校验这�?check ID 存在且不重复、不出现未知项，`blockers/warnings` 计数与明细一致、`status` 与是否存�?blocker 一致，并要求每项带有可�?`detail` 和对�?`envKeys`；任何漂移都会进�?strict release gate�?

Frontend smoke evidence 会输�?`flowCoverage`，逐条标记必需 flow �?`passed`/`missing` 状态、匹配到�?Playwright 标题，以及缺失原因。artifact 还会写入 `diagnostics.playwrightReport` �?`diagnostics.staticSpecCoverage`，用于区分“Playwright JSON 未生�?未上传”和“required flow 没有写入 `@smoke` spec”。contract 会要求动�?flow 覆盖和静�?spec 覆盖都与 required-flow 清单精确同步。Playwright JSON 缺失、测试未运行或某�?flow 没有 passed `@smoke` 用例时，strict gate 仍会阻断发布，但发布负责人可以直接从 `flowCoverage` 和静态覆盖诊断定位缺口�?

运行时证据统一要求：发布流水线生成 readiness、release config、authenticated performance、File、Payment、Outbox、Job、AI �?Frontend smoke artifact 时，应设�?`DDD_EVIDENCE_ENVIRONMENT`、`DDD_RELEASE_CANDIDATE`、`DDD_EVIDENCE_OPERATOR`。strict 模式下这�?smoke/evidence 脚本会在生成阶段拒绝缺失或占�?provenance；strict release gate 也会再次要求 runtime artifact 具备非占�?`sourceEnvironment`、`releaseCandidate`、`evidenceOperator`，防止证据无法追溯到环境、版本和执行人。Readiness、Authenticated performance、File、Payment、Job、AI �?Frontend 运行证据会写�?`productionEquivalence`：本�?HTTP artifact 可以保留为开发调试证据，�?strict 发布必须使用 HTTPS 且非 localhost 的准生产/生产等价 baseURL，并在需要时通过 owner-specific `DDD_*_DEPLOYMENT_EVIDENCE` 或通用部署证据变量附加部署/变更�?日志引用�?

对于已经存在但缺 provenance 的历�?runtime artifact，可�?`DDD_RUNTIME_PROVENANCE_BACKFILL=true DDD_EVIDENCE_ENVIRONMENT=... DDD_RELEASE_CANDIDATE=... DDD_EVIDENCE_OPERATOR=... node bin/ddd-backfill-runtime-provenance.mjs` 进行 metadata-only 补录。对于缺结构�?`productionEquivalence` 的旧 artifact，可�?`DDD_RUNTIME_PRODUCTION_EQUIVALENCE_BACKFILL=true node bin/ddd-backfill-runtime-provenance.mjs` 基于 `baseUrl` 保守补录。该工具只补来源字段、production-equivalence 元数据和 backfill 审计字段，不改变证据状态、失败列表或性能结果；它不会解除 localhost、HTTPS、生产等价环境或新鲜�?blocker�?

证据新鲜度统一要求：strict release gate 默认要求关键 artifact �?`generatedAt`、`checkedAt`、`finishedAt` �?`startedAt` 距离执行门禁不超�?24 小时，EXPLAIN JSON 以文�?mtime 校验。可�?`DDD_RELEASE_MAX_ARTIFACT_AGE_HOURS` 调整窗口；authenticated performance baseline 是已验收历史基线，不参与 freshness 校验�?

## 2. 原始方案到证据映�?

| 原始要求 | 证据 |
| --- | --- |
| DDD 基座和架构护�?| `DddArchitectureBoundaryTest`、`backend-test-evidence.json`、CI 架构测试；必需测试类不�?0 tests 或全�?skipped |
| 聚合启动和独�?owner service 可构�?| `backend-build-evidence.json`、Maven package 产物 |
| 可部署容器镜�?| `docker-image-evidence.json`、CI Docker image job |
| `/api/v2/**` �?v1 adapter 兼容 | �?owner v2 controller/readiness tests、runtime readiness artifact |
| Flyway 新库启动与旧库升级测�?| `migration-evidence.json`、Flyway 日志、`flyway_schema_history` 证据 |
| 生产等价配置完整�?| `release-config-evidence.json`，只记录变量存在性、脱�?URL �?secret hash 前缀，不泄露密钥 |
| 运行时证据可追溯 | runtime smoke artifact 顶层 `sourceEnvironment`、`releaseCandidate`、`evidenceOperator` |
| 发布证据新鲜�?| artifact 时间戳、EXPLAIN JSON mtime、`DDD_RELEASE_MAX_ARTIFACT_AGE_HOURS` |
| 发布证据防篡改清�?| `evidence-manifest.json` �?artifact SHA-256、大小、时间戳 |
| 命令路径事件化、查询路�?read model/cache | owner domain/app tests、outbox replay/dead-letter artifact、authenticated performance artifact |
| 高频缓存键、版本化快照 | IAM/Auth/Platform/Localization tests、runtime metrics artifact |
| Outbox relay 批量 claim、退避、幂等、死信、replay | `outbox-replay-dead-letter-test-evidence.json`、`job-e2e-smoke.json` |
| 列表分页、owner 索引、禁止无�?count | 架构测试、EXPLAIN artifact、`doc/28-ddd-hot-path-explain-plan.md` |
| 性能 baseline 不被本地证据污染 | `authenticated-runtime-baseline.json` metadata、`ddd-promote-performance-baseline.mjs`、strict release gate |
| File 上传不承担重处理 | `file-processing-e2e.json`、authenticated upload timing |
| Payment webhook 幂等可靠 | `payment-webhook-e2e.json` |
| AI 长任务异步化、provider/owner 解�?| `ai-runtime-drill.json`、AI provider runtime tests、AI owner gateway tests |
| 前端主流程兼�?| `lumira-ui-smoke.json`、Playwright HTML/trace/video artifact |
| 前端生产构建可部�?| `lumira-ui-build-evidence.json`、`lumira-ui/dist` 产物 |
| 前端静态质量和单元回归 | `lumira-ui-static-evidence.json`、CI lumira-ui job |
| 每个上下文有回滚方案和演练证�?| `rollback-drill.json`、readiness rollbackSteps、运行手册演练记�?|
| 未来物理拆服务准�?| `physical-split-readiness.json`、`doc/29-ddd-physical-split-readiness.md` |

## 3. 发布判定

- `node bin/ddd-release-evidence-gate.mjs`：本�?advisory，只汇总缺口，不代表可发布；readiness summary 会显�?`Release gate mode: advisory`，若仍有 blocker 则状态是 `NOT_READY`，只�?gate 存在�?blocker/contract issue 均为 0 时才会标记为 `ADVISORY`�?
- `DDD_RELEASE_EVIDENCE_STRICT=true node bin/ddd-release-evidence-gate.mjs`：准生产/发布流水线阻断门禁；缺失 orchestrator run 报告、orchestrator preflight `BLOCKER`、缺失后端构�?测试证据、缺�?Docker 镜像构建证据、缺失前端构�?测试证据、缺�?Flyway 新库/旧库运行验证、缺失生产等价配置矩阵、缺�?checksum manifest、localhost-only 运行�?artifact、运行时 artifact 缺少来源元数据、前�?smoke 未声明部署环境或 baseURL �?HTTPS、artifact 超过 freshness 窗口、缺失性能 baseline、baseline 来源�?localhost 或缺少验收元数据、超�?10% 性能回退或缺失回滚演练证据都会被视为不合格证据。gate 产物自身也会通过共享 contract 复核 `summary` �?`checks[]`、`blockers[]`、`warnings[]` 明细一致，拒绝非法 check 状态或缺少 name/detail 的条目�?
- strict 通过后仍需保留部署版本、数据库 migration 版本、配置版本、回滚窗口和相关日志索引，作为变更审计的一部分�?

## 4. GitHub Actions 手动证据采集

仓库提供 `.github/workflows/ddd-release-evidence.yml` 作为准生�?生产等价证据采集入口。该 workflow 通过 `workflow_dispatch` 手动触发，支持两种模式：

- `mode=plan`：只生成 `orchestrator-report.json`，列出将要执行的证据脚本，不运行重型构建和运行时 smoke�?
- `mode=run`：按 orchestrator 顺序先执行配置矩阵预检，再执行后端测试、构建、Docker 镜像、前端构建、迁移、运行时 smoke、可�?authenticated performance baseline 晋级、AI drill、前�?smoke、回滚校验、EXPLAIN、manifest、strict gate �?readiness summary�?

`promote_authenticated_baseline=false` 为默认值。首次建立或批准刷新性能基线时，发布负责人必须显式设�?`promote_authenticated_baseline=true`，并填写 `baseline_accepted_by`；否�?orchestrator 会把 baseline 晋级步骤记录�?skipped，strict gate 仍要求已�?`authenticated-runtime-baseline.json` 通过校验�?

触发前需要在对应 GitHub environment 中配置生产等�?secret。secret 名称�?workflow 中的环境变量映射一致，主要包括�?

- 数据与缓存：`LUMIRA_DB_URL`、`LUMIRA_DB_USERNAME`、`LUMIRA_DB_PASSWORD`、`LUMIRA_REDIS_HOST`、`LUMIRA_REDIS_PORT`、`LUMIRA_REDIS_PASSWORD`�?
- 安全与入口：`LUMIRA_JWT_SECRET`、`LUMIRA_FIELD_SECRET`、`backend_base_url`、`frontend_base_url`�?
- owner 服务：`SYSTEM_SERVICE_BASE_URL`/`LUMIRA_SYSTEM_SERVICE_BASE_URL`、`AUTH_SERVICE_BASE_URL`/`LUMIRA_AUTH_SERVICE_BASE_URL`、`FILE_SERVICE_BASE_URL`/`LUMIRA_FILE_SERVICE_BASE_URL`、`MESSAGE_SERVICE_BASE_URL`/`LUMIRA_MESSAGE_SERVICE_BASE_URL`、`PLUGIN_SERVICE_BASE_URL`/`LUMIRA_PLUGIN_SERVICE_BASE_URL`、`LOCALIZATION_SERVICE_BASE_URL`/`LUMIRA_LOCALIZATION_SERVICE_BASE_URL`、`PAYMENT_SERVICE_BASE_URL`/`LUMIRA_PAYMENT_SERVICE_BASE_URL`、`AI_SERVICE_BASE_URL`/`LUMIRA_AI_SERVICE_BASE_URL`、`JOB_EXECUTOR_BASE_URL`/`LUMIRA_JOB_EXECUTOR_BASE_URL`�?
- Job/Outbox：`SAAS_JOB_INTERNAL_TOKEN`/`LUMIRA_JOB_INTERNAL_TOKEN`、`SAAS_JOB_BACKEND_BASE_URL`/`LUMIRA_JOB_BACKEND_BASE_URL`、`SAAS_JOB_MESSAGE_SERVICE_BASE_URL`/`LUMIRA_JOB_MESSAGE_SERVICE_BASE_URL`、`SAAS_JOB_FILE_SERVICE_BASE_URL`/`LUMIRA_JOB_FILE_SERVICE_BASE_URL`、`SAAS_JOB_PAYMENT_SERVICE_BASE_URL`/`LUMIRA_JOB_PAYMENT_SERVICE_BASE_URL`、`SAAS_JOB_PLUGIN_SERVICE_BASE_URL`/`LUMIRA_JOB_PLUGIN_SERVICE_BASE_URL`、`XXL_JOB_ADMIN_ADDRESSES`/`LUMIRA_XXL_JOB_ADMIN_ADDRESSES`、`XXL_JOB_ACCESS_TOKEN`/`LUMIRA_XXL_JOB_ACCESS_TOKEN`、`SAAS_EVENT_OUTBOX_DISPATCHER`/`LUMIRA_EVENT_OUTBOX_DISPATCHER`、`SAAS_EVENT_REDIS_STREAM_KEY`/`LUMIRA_EVENT_REDIS_STREAM_KEY`�?
- File/Payment/AI：`UPLOAD_STORAGE_ROOT`/`LUMIRA_UPLOAD_STORAGE_ROOT`、`LUMIRA_FILE_SECURITY_SCAN_MODE`、`LUMIRA_FILE_OCR_MODE`、`PAYMENT_WEBHOOK_SECRET`/`LUMIRA_PAYMENT_WEBHOOK_SECRET`、`LUMIRA_AI_PROVIDER_OPENAI_COMPATIBLE_ENABLED`/`LUMIRA_AI_PROVIDER_ENABLED`、`LUMIRA_AI_PROVIDER_OPENAI_COMPATIBLE_BASE_URL`/`LUMIRA_AI_PROVIDER_BASE_URL`、`LUMIRA_AI_PROVIDER_OPENAI_COMPATIBLE_API_KEY`/`LUMIRA_AI_PROVIDER_API_KEY`、`LUMIRA_AI_PROVIDER_OPENAI_COMPATIBLE_CHAT_MODEL`/`LUMIRA_AI_CHAT_MODEL`、`LUMIRA_AI_PROVIDER_OPENAI_COMPATIBLE_EMBEDDING_MODEL`/`LUMIRA_AI_EMBEDDING_MODEL`、`LUMIRA_AI_OWNER_INTEGRATIONS_INTERNAL_TOKEN`/`LUMIRA_AI_OWNER_INTERNAL_TOKEN`、`LUMIRA_AI_OWNER_INTEGRATIONS_IAM_BASE_URL`/`LUMIRA_AI_OWNER_IAM_BASE_URL`、`LUMIRA_AI_OWNER_INTEGRATIONS_PLATFORM_BASE_URL`/`LUMIRA_AI_OWNER_PLATFORM_BASE_URL`、`LUMIRA_AI_OWNER_INTEGRATIONS_FILE_BASE_URL`/`LUMIRA_AI_OWNER_FILE_BASE_URL`�?

workflow 会生成临�?`.release-evidence.env` 并设�?`DDD_RELEASE_ENV_FILE`，不会把 secret 写入仓库。无�?orchestrator 成功或失败，都会上传 `artifacts/ddd/**`、`tmp/ddd-explain/**` �?Playwright 报告目录，便于下载审计。`promote_authenticated_baseline=false` 是默认值，用于防止每次发布流水线自动把当前 actual 当作 baseline；只有发布负责人明确批准刷新基线时才打开。`mode=run` �?`strict=true` 时，任何 blocker 都会让最终步骤失败；这是发布保护，而不�?CI 异常�?

`mode=run` �?`strict=true` 会先生成临时 `.release-evidence.env`，执�?`ddd-release-env-file-lint` �?`ddd-release-config-evidence` 预检；如果基础 URL、DB/Redis、secret、owner service、Job、AI、Payment 等发布配置不完整，或 env 文件仍含占位符，会在重型构建、Docker、Playwright �?runtime smoke 前快速失败并上传配置证据。预检通过后再安装前端依赖�?Playwright Chromium，并执行 orchestrator。workflow 会在上传 artifact 前刷�?final go/no-go packet，并在存�?`release-final-owner-queue-run-report.json` 时自动运�?run report contract；`ddd-release-artifact-path-leak-contract.mjs` 默认扫描 21 个关�?JSON/CSV/Markdown release evidence，包�?`readiness-summary.md`、`release-final-go-no-go.md`、`release-config-owner-input-reconciliation.json`、`release-owner-input-receipt.json`、`release-owner-input-receipt.csv`、`release-owner-input-receipt-items.csv`、`release-owner-input-receipt-items.md`、`release-owner-input-receipt.md`、`release-unblock-brief.md`、`evidence-manifest-preflight.json` �?`release-performance-baseline-closure.md`，并拒绝�?runner/repo/home 绝对路径的内容，防止本机路径进入上传证据包；`release-preflight-gate.sh` 会先生成 manifest provenance preflight report，再运行 path leak contract 扫描�?report，strict 模式会在 path leak 通过后对 manifest provenance 缺口早停；它还会运行 `ddd-release-env-owner-handoff-redacted-contract.mjs`，确�?redacted owner handoff �?JSON/CSV/Markdown/目录文件没有 stale owner、路径穿越或具体值泄露，然后才进�?env readiness/final go-no-go；preflight 默认/strict 输出会在上传 artifact 前原地经�?`node bin/ddd-release-redact-output.mjs`，去除具�?release env 文件路径、secret assignment �?runner/repo 绝对路径，`ddd-release-preflight-capture-contract.mjs` 会拒绝未脱敏的捕获文件，并在 `release-preflight-capture-contract.json` 写入 `redacted=true` �?redactionPolicy；随后把 `artifacts/ddd/release/readiness-summary.md`、`artifacts/ddd/release/release-final-go-no-go.md` 和脱敏后�?preflight 输出追加�?GitHub Step Summary。发布负责人可以直接�?Actions 页面查看 blocker 分类、owner、下一步动作和最终切流结论；strict run 的最�?enforce 步骤会调�?`DDD_FINAL_GO_NO_GO_ENFORCE=1 bash artifacts/ddd/release/release-final-go-no-go-gate.sh`，`cutoverAllowed=false` 时以退出码 10 阻断切流�?

`artifacts/ddd/release/release-env-owner-input-packet.json`、`.csv`、`.md` 和�?owner 目录文件现在都包�?`postCollectionReceipt`。owner 填完真实值后必须�?receipt commands 重新�?safe defaults、provenance defaults、alias sync、canonical/env lint、config evidence、readiness summary、config-owner reconciliation、preflight �?final go/no-go；验收口径是 `releaseEnvReadinessStatus=PASS`、`releaseEnvReadinessBlockers=0`、`releaseEnvReadinessPlaceholders=0`、`releaseEnvReadinessMissing=0`、`configOwnerInputReconciliationStatus=PASS` �?`configOwnerInputReconciliationUnmappedKeys=0`。这�?receipt 只列命令和通过标准，不写真�?env 值或 secret�?

`artifacts/ddd/release/release-fast-track.json` �?`.md` 是快速上线决策工件，只给出最短安全路径，不改�?strict gate 规则。它会输�?`recommendation=NO_GO_STRICT` �?`GO_STRICT`、`noAutoWaivers=true`、不可豁免安�?lane、可并行证据 lane、当�?ready/blocked batch、env alias group 和机器可�?`cutoverChecklist`。快速上线时先看它确认哪些事项不能豁免，再用 execution queue 执行当前 ready batch；不得用 fast-track 工件替代 release gate。`cutoverChecklist` 会把 strict gate、环境配置、镜像、生产等价、迁移、运行时/业务验收、回滚、EXPLAIN、manifest/orchestrator 复核逐项标成 `PASS` �?`BLOCKED`，上线切流前必须全部�?`PASS`；`release-cutover-checklist.csv` 是同一 checklist 的表格版，适合导入发布审批或切流会议清单。`release-cutover-owner-matrix.json`、`.csv` �?`.md` 是同一 checklist �?owner 分派视图，会�?batch 自动带出 owner、命令、env alias group、预期证据和退出标准，适合把阻塞项直接分派�?release-infra、database、lumira-ui、AI、File、Payment �?owner，且不会绕开 `noAutoWaivers=true`。`release-sprint-board.json`、`.csv` �?`.md` 是快速上线战情看板，会按 priority、owner �?ready/blocked 状态汇�?batch card，并把依赖、cutover 项、命令、env alias group、预期证据和退出标准放到同一行，适合 standup、Jira/飞书导入和下一波并行执行；`release-command-catalog.json`、`.csv` �?`.md` 会把同一 next wave 生成 owner、priority、batch 三种粒度�?list、env-check、dry-run �?execute 命令，适合直接复制到发布群或工单。`release-owner-handoff.json`、`.csv` �?`.md` 会把 sprint board、command catalog �?env owner matrix 合并�?owner 级交接包，逐个 owner 展示 ready/blocked batch、阻塞依赖、env keys、可复制命令、预期证据和退出标准，适合快速创建工单、飞书任务或发布群分派。`release-owner-receipts.json`、`.csv` �?`.md` 是执行后�?owner 回执视图，会检�?handoff 中的预期 artifact 是否已落盘，并合�?owner action blocker，区�?`ARTIFACT_MISSING`、`CONTENT_BLOCKED`、`READY_FOR_STRICT_GATE_RERUN` �?`WAITING_ON_DEPENDENCIES`；它用于快速定位下一步，不替�?strict release gate。`release-next-action-queue.json`、`.csv` �?`.md` 会把 receipts 压成�?owner 排序的下一步队列，优先展示可立即执行的�?artifact 和内�?blocker，适合发布负责人直接分派当前最短处理顺序，并通过 `executableCommands` 提供可复制执行入口；`release-next-action-commands.sh` 会按 RUN_NOW 队列生成安全默认 dry-run 脚本，支�?`DDD_RELEASE_NEXT_ACTION_LIST=1` 只列队列、`DDD_RELEASE_NEXT_ACTION_DETAIL=1` 查看单项详情，并支持 `DDD_RELEASE_NEXT_ACTION_ORDER`、`DDD_RELEASE_NEXT_ACTION_OWNER` 过滤，只有设�?`DDD_RELEASE_NEXT_ACTION_EXECUTE=1` 才实际执行；它同样不能作�?waiver。`release-blocker-closure-plan.json`、`.csv` �?`.md` 会把每个 priority action 归类�?`RUN_NOW_LOCAL`、`RUN_NOW_WITH_REAL_ENV` �?`WAIT_FOR_DEPENDENCIES`，并列出 owner、batch、依赖、env keys、命令、预期证据和退出标准，适合发布负责人快速区分本地可推进项与必须真实 HTTPS/CI/secret 环境采集的项；它只派生自 priority/batch，不新增 waiver。`release-env-owner-matrix.json`、`.csv` �?`.md` 会把缺失发布环境变量�?owner 汇总，并带�?canonical key、alias mapping、ready/blocked batch、命令、预期证据和退出标准，适合分派 `.env.release`、GitHub environment secret �?CI secret 补齐工作；它只列 key 和命令，不写 secret。它们只�?fast-track、batches、execution queue、missing env �?owner matrix 派生，不�?waiver。`artifacts/ddd/release/release-action-batches.json`、`.csv` �?`.md` 会把待办�?priority、source、owner 分批。每�?batch 必须包含稳定 `id`、`dependsOn`、`canRunImmediately`、`commands`、`envKeys`、`envCheckGroups`、`expectedArtifacts` �?`exitCriteria`，JSON、CSV �?Markdown 都会展示这些字段：P0 批次可以并行准备，P1/P2/P3 只有�?`dependsOn` 指向的前置批次退出标准满足并重新运行 release gate 后再执行，最后再�?P3 strict orchestrator 复核。全�?`release-action-batches.csv` 用于把所有批次导入工单或发布排期表，`release-execution-queue.csv` 则只表达当前 ready/blocked 执行队列。`artifacts/ddd/release/release-execution-queue.json`、`.md`、`.csv` �?`release-execution-commands.sh` 是发布执行入口，`Ready Now` 只列当前可立即执行的批次，`Blocked Later` 列被前置证据阻塞的批次和 unmet dependencies；CSV �?`queueStatus` 区分 ready/blocked，并�?`dependsOn`、`commands`、`envCheckGroups`、`expectedArtifacts`、`exitCriteria` 展平成可导入工单或表格的列；`.sh` 会切到仓库根目录（支�?`LUMIRA_REPO_ROOT` 覆盖），`DDD_RELEASE_LIST_BATCHES=1` 可在不提�?secret 的情况下列出当前 ready batch，并支持同一�?`DDD_RELEASE_BATCH`、`DDD_RELEASE_OWNER`、`DDD_RELEASE_PRIORITY` 过滤器先预览执行范围；正式执行时会检�?`DDD_RELEASE_ENV_FILE` 已设置、文件存在且不是 `release-env-missing.template.env`，然后用 safe dotenv loader 加载真实发布配置；该 loader 只接�?`KEY=value` �?`export KEY=value`，不会执�?env 文件中的 shell 语句；设�?`DDD_RELEASE_BATCH=<batch-id>` 可以只执行一�?ready batch，设�?`DDD_RELEASE_OWNER=<owner>` 可以只执行该 owner 当前 ready batch，设�?`DDD_RELEASE_PRIORITY=P0|P1|P2|P3` 可以只执行该优先级当�?ready batch，多条件组合时必须同时匹配，未匹配会失败；设�?`DDD_RELEASE_DRY_RUN=1` 可预览命令但不会生成证据；设�?`DDD_RELEASE_CHECK_ENV_ONLY=1` 可只运行 env group 预检并跳过证据命令，适合发布负责人先确认 GitHub environment secret �?`.env.release` 注入完整性；缺少任何 env group 默认会失败，只有显式设置 `DDD_RELEASE_ALLOW_MISSING_ENV=1` 才会继续，这个开关只用于本地诊断，不能用于正式上线。env group 预检使用 release config contract 的别名组，例�?`LUMIRA_BASE_URL=DEPLOY_CHECK_BASE_URL|LUMIRA_BASE_URL`、`DB_USERNAME=DB_USERNAME|MYSQL_USER|SPRING_DATASOURCE_USERNAME`，组内任�?key 存在就视为该组覆盖。该脚本只生成命令提示，不写 secret，不作为发布证据。`artifacts/ddd/release/release-action-dependency-graph.json` �?`.md` 会从同一批次数据生成 `nodes`、`edges`、`readyBatchIds`、`blockedBatchIds`、`byPriority`、`executionLevels`、`graphDensity` �?`compressedEdges`，每�?node 也携带同批次 `envKeys` �?`envCheckGroups`；Markdown 先显示按 priority 聚合的压�?Mermaid 图，再显示完�?DAG，适合先分派可并行 P0 工单并识别仍被依赖阻塞的 P1/P2/P3 工单。`commands` 由固�?source/owner 证据流程和行动项里的命令共同生成，确�?authenticated performance、lumira-ui smoke、manifest、orchestrator 等批次不会漏掉前置采集命令。`expectedArtifacts` 是执行后应归档或刷新的最小证据集合，并按 owner 精确到产物，例如 File/Job/Payment business E2E owner 分别只要求自己的 E2E artifact；manifest 批次还会列出实际缺失 artifact 本身，authenticated runtime baseline 缺失时会同时要求 baseline �?promotion audit。`exitCriteria` 是进入下一批前的人工验收门槛，不能只看命令退出码�?

`artifacts/ddd/release/release-owner-input-receipt.json`、`artifacts/ddd/release/release-owner-input-receipt.csv`、`artifacts/ddd/release/release-owner-input-receipt-items.csv`、`artifacts/ddd/release/release-owner-input-receipt-items.md` �?`artifacts/ddd/release/release-owner-input-receipt.md` 会把 owner input collection 的当前验收状态输出成脱敏回执；`.csv` 一行一�?owner，`-items.csv` 一行一�?canonical key，`-items.md` 是同一逐项清单�?Markdown checklist，便于导入工单或表格逐项关闭 34 个输入；`release-owner-input-receipt-items/` 目录会再�?owner 拆出逐负责人 checklist，适合直接分派�?platform-events、platform-owners、release-infra、ai-owner �?payment-owner。所有回执只包含 owner、key 名、alias、输入数量、remaining placeholders/missing、packet/handoff 相对路径�?pass criteria，不包含 secret 或真�?env 值�?

`release-next-action-queue.json`、`.csv` �?`.md` 也会展示 owner input receipt �?`PASS/PENDING_OWNER_INPUT`、pending owner、missing criteria、required owner input 数和�?owner checklist 路径；contract 会要求这些字段自洽，并要�?`cutoverAllowed=true` �?receipt 必须 `PASS` �?`cutoverReady=true`。它只用于分派和排障，不�?waiver�?

`release-blocker-closure-plan.json`、`.csv` �?`.md` 也会展示同一�?owner input receipt 摘要，包�?pending owner、missing criteria �?required owner input 数；readiness summary contract 会要�?closure plan �?next-action queue �?receipt 一致。它只帮�?owner 分派和关�?blocker，不允许绕过 final go/no-go�?

`release-next-action-commands.sh` �?dry-run、list �?detail 模式不需�?secret；设�?`DDD_RELEASE_NEXT_ACTION_CHECK_ENV=1` 时只�?safe dotenv loader 读取真实 `DDD_RELEASE_ENV_FILE` 并检查当�?RUN_NOW 项缺少哪�?key，不执行证据命令、不输出 secret 值。正式执行时必须设置 `DDD_RELEASE_NEXT_ACTION_EXECUTE=1` 且提供真�?`DDD_RELEASE_ENV_FILE`。脚本会拒绝缺失文件�?`release-env-missing.template.env`，再用同一 safe dotenv loader 解析 env 文件执行队列命令；该 loader 只接�?`KEY=value` �?`export KEY=value`，不会执�?env 文件中的 shell 语句。批量诊断时可显式加 `DDD_RELEASE_NEXT_ACTION_CONTINUE_ON_ERROR=1` 继续执行后续命令；只要任一命令失败，最终仍会非零退出，不能作为上线成功依据�?

`release-blocker-closure-commands.sh` �?blocker closure plan 的命令入口，默认只列 `RUN_NOW_LOCAL` �?`RUN_NOW_WITH_REAL_ENV` 项，不执行。可�?`DDD_RELEASE_CLOSURE_DETAIL=1` 查看单项详情，用 `DDD_RELEASE_CLOSURE_CHECK_ENV=1` 只检查所需 key，用 `DDD_RELEASE_CLOSURE_EXECUTE=1` 执行；支�?`DDD_RELEASE_CLOSURE_ORDER`、`DDD_RELEASE_CLOSURE_OWNER`、`DDD_RELEASE_CLOSURE_PRIORITY`、`DDD_RELEASE_CLOSURE_KIND` 过滤。env-check �?execute 都必须提供真�?`DDD_RELEASE_ENV_FILE`，并拒绝模板文件；脚本只打印 key 名，不打�?secret 值。批量诊断时可显式加 `DDD_RELEASE_CLOSURE_CONTINUE_ON_ERROR=1` 继续执行后续命令；只要任一命令失败，最终仍会非零退出�?

`release-execution-commands.sh` �?`release-final-owner-queue-commands.sh` �?continue-on-error 开关分别是 `DDD_RELEASE_CONTINUE_ON_ERROR=1` �?`DDD_FINAL_OWNER_QUEUE_CONTINUE_ON_ERROR=1`。它们只用于快速收集完整失败面；脚本会累计失败并在结尾非零退出，不能替代 strict gate、manifest、性能、迁移、回滚或生产等价证据�?

`release-execution-commands.sh` 真实执行模式会生�?`release-execution-run-report.json`，记�?batch/owner/priority 过滤条件、整体退出码、`summary.totalEntries/succeededEntries/failedEntries` 汇总计数，以及每条命令�?`batchId`、`owner`、`priority`、退出状态、耗时和完成时间；可用 `DDD_RELEASE_EXECUTION_REPORT` 指定其他安全路径。dry-run �?env-check-only 不写该报告，避免把预演误当成真实执行证据。脚本写出报告后会自动调�?`node bin/ddd-release-execution-run-report-contract.mjs` 校验 schema、`PASS/FAIL` 与退出码一致性、summary 计数�?entries 明细一致性，并拒绝命令里出现未脱�?secret、release env 文件路径或本机仓库绝对路径。`node bin/ddd-release-execution-run-report-summary.mjs` 可把该报告追加到 GitHub Step Summary 或输�?Markdown，方便发布负责人�?batch/owner/priority 快速定位失败命令。该报告用于排障、审计和 owner 复盘，不替代任何 expected artifact、strict release gate �?final go/no-go�?

`release-closure-wave-env-matrix.json`、`.csv` �?`.md` 会按 closure wave 汇�?owner、batch、env keys、命令和预期证据；`release-closure-wave-env.template.env` 是按 wave 分段的安全模板，只放 key �?`__REQUIRED__` 占位符，不放 secret 值。真实环境执行前先按该模板补�?`.env.release`，再�?`DDD_RELEASE_CLOSURE_CHECK_ENV=1` 预检�?

`release-closure-wave-receipts.json`、`.csv` �?`.md` 会按 wave 检�?expected artifacts 是否已经落盘，并标出 `ARTIFACT_MISSING`、`CONTENT_BLOCKED` �?`READY_FOR_STRICT_GATE_RERUN`；`CONTENT_BLOCKED` 表示 artifact 已存在但 provenance、baseline 或内容合同仍未满足，`blockerHints` 会保留具体原因且不会被误列为缺失文件路径。每�?wave 都带 `node bin/ddd-release-evidence-gate.mjs` �?`node bin/ddd-release-readiness-summary.mjs` 复核命令。它只帮助执行后验收，不替代 strict gate。`release-final-go-no-go.*` �?`release-final-owner-queue.*` 会继续透传 content blockers，方�?CI 日志�?owner 分派直接定位内容问题�?

`release-closure-wave-blocker-map.json`、`.csv` �?`.md` 会把 closure wave �?priority action、source-derived category、候�?blocker hint、命令和预期证据关联起来。该映射用于执行后解�?gate 变化，不�?waiver；只有重新运�?strict release gate �?blocker 实际消失，才算关闭�?

`release-performance-baseline-closure.json` �?`.md` 专门收敛 authenticated performance baseline：它会列出当�?actual、baseline promotion、baseline 缺口、不能晋级的 blocker、所需 env keys 和推荐命令。baseline 只能从生产等价、非本地、无失败�?actual 晋级，不能手工伪造�?

`release-performance-baseline-commands.sh` 是同一闭环的安全执行入口：默认输出 detail/dry-run，`DDD_AUTH_PERF_BASELINE_CHECK_ENV=1` 只检�?env key，`DDD_AUTH_PERF_BASELINE_EXECUTE=1` 才执行性能采集、baseline 晋级、manifest、release gate �?readiness summary。它必须使用真实 `DDD_RELEASE_ENV_FILE`，拒绝生成模�?env 文件，并要求真实 env 文件权限收紧�?`600`�?

`release-final-go-no-go.json`、`.csv` �?`.md` 会把 strict gate、fast-track、closure wave 回执、closure blocker map 和性能 baseline closure 汇总成最终发布包；JSON �?CSV 都同时输�?`recommendation` 与兼容字�?`finalRecommendation`。发布切流只�?`cutoverAllowed=true`，且该字段只能由 strict gate 0 blocker、cutover �?PASS、wave 回执�?ready、性能 baseline READY 共同推出；任�?stop reason 都必须回到对�?owner 证据修复，不能人工改文件放行�?

`release-final-go-no-go-gate.sh` 会读取同一 final packet。默认模式用于人工查看；CI/CD 切流步骤必须设置 `DDD_FINAL_GO_NO_GO_ENFORCE=1`，NO-GO 时脚本以退出码 10 失败，GO 时退�?0。该脚本只消费已生成证据，不会自动补证据或绕�?strict gate�?

final packet �?`ciSummary` 会固定输�?`enforceCommand`、`nonGoExitCode=10`、`stopOwners`、`blockedArtifactPaths`、`firstNextCommand` �?rerun 命令。发布失败时先看 `stopOwners` 分派 owner，再�?`blockedArtifactPaths` 确认缺失证据，最后执�?`firstNextCommand` 或对�?owner wave 命令�?

`release-final-owner-queue.json`、`.csv` �?`.md` 会把 final go/no-go �?stop owners 进一步展开�?owner 级工单队列：每个 owner 包含稳定 `queueOrder`、`canExecute`、命�?env/artifact/stop reason 计数、cutover item、ready/blocked batch、closure wave、缺�?artifact、首条命令和 rerun 命令。队列顶层还会展�?owner input receipt �?`PASS/PENDING_OWNER_INPUT`、pending owner 数、missing criteria、required owner input 数和�?owner checklist 路径；`cutoverAllowed=true` 时该 receipt 必须�?`PASS` �?`cutoverReady=true`。CI 失败后优先把�?CSV 或对�?checklist 导入工单系统，按 `queueOrder` �?`canExecute=true` 分派，owner 完成证据后再重跑 final gate�?

`release-final-owner-queue-commands.sh` 是同一 owner queue 的安全执行入口。默认只�?`ACTIONABLE` owner；可�?`DDD_FINAL_OWNER_QUEUE_OWNER=<owner>`、`DDD_FINAL_OWNER_QUEUE_STATUS=ACTIONABLE|WAITING` �?`DDD_FINAL_OWNER_QUEUE_DETAIL=1` 查看�?owner 明细；设�?`DDD_FINAL_OWNER_QUEUE_CHECK_ENV=1` 时只静态解析真�?`DDD_RELEASE_ENV_FILE` 并检查该 owner 所需 env key，不 `source` 文件、不执行证据命令；`WAITING` owner 允许 detail/check env 提前准备，但 `DDD_FINAL_OWNER_QUEUE_EXECUTE=1` 会拒绝执行，必须先关闭依�?blocker；真�?env 文件权限必须�?`600`，否�?check/execute 都会拒绝继续；只有设�?`DDD_FINAL_OWNER_QUEUE_EXECUTE=1` 才加�?env 并执行命令。执行后仍必须重�?strict gate、readiness summary �?final go/no-go gate�?

执行模式会生�?`release-final-owner-queue-run-report.json`，记�?owner/status 过滤条件、整体退出码、`summary.totalEntries/succeededEntries/failedEntries` 汇总计数，以及每条命令�?`commandIndex/commandCount`、退出状态和耗时；可�?`DDD_FINAL_OWNER_QUEUE_REPORT` 指定其他安全路径。该报告是排障和审计辅助，不替代�?evidence artifact �?strict gate�?

执行脚本写出报告后会自动调用 `node bin/ddd-final-owner-queue-run-report-contract.mjs` 校验 run report �?schema、`PASS/FAIL` 与退出码一致性、`summary` 计数�?`entries` 明细一致性，以及每条命令 entry �?owner、queueOrder、queueStatus、commandIndex、commandCount、command、status、duration �?finishedAt；该脚本也可单独复核历史报告。`queueOrder/queueStatus` 用于追溯该命令为什么属于当前执行批次，`commandIndex/commandCount` 用于定位 owner 内第几条命令失败，防�?owner 回执脱离 final queue 上下文。该检查只验证审计报告可消费，不作为发布放行证据�?

`release-final-owner-queue-env.template.env` 会按 owner 分段列出 final owner queue 所需 env key，值统一�?`__REQUIRED__`，不会包�?secret；每�?owner 段也会标�?`queueOrder` �?`canExecute`，方�?secret 填写人先处理当前可执�?owner。执行人可按该模板补齐安全位置中�?`.env.release`，再�?`DDD_FINAL_OWNER_QUEUE_CHECK_ENV=1` 预检单个 owner�?

`release-final-owner-queue-env-init.sh` 可从模板初始化本地真�?env 文件，默认写�?`.env.release.local` �?`DDD_RELEASE_ENV_FILE`，权限固定为 `600`，并默认拒绝覆盖已有文件；初始化成功后的下一步提示会指向当前 `queueOrder` 最靠前�?`canExecute=true` �?owner，而不是固�?owner。不要把初始化后填入真实值的文件提交到仓库�?

`bin/ddd-release-config-sync.test.mjs` 会校�?`releaseConfigGroups` 中的必填项在 `doc/36-ddd-release-env-template.env` �?`.github/workflows/ddd-release-evidence.yml` 中都有至少一个对应变量，并校�?workflow 的配置预检位于前端依赖安装�?Docker setup 之前，避免新增发布配置要求时漏同�?CI secret 或模板。`release-config-evidence.json` 还会输出 `coverageMatrix`，按 check 展示 runtime、env file、template �?workflow 四层覆盖；readiness summary 会显�?required/runtimePresent/envFile/template/workflow 覆盖计数，帮助区分“发布模板已覆盖但本次运行没有注�?env”和“模板或 workflow 漏键”�?

`bin/ddd-dockerfile-contract.test.mjs` 会在不依�?Docker daemon 的情况下静态校�?`deploy/docker/service.Dockerfile` �?`deploy/docker/lumira-ui.Dockerfile`：server 镜像必须保留 `SERVICE_MODULE`/`SERVICE_DIR`、非 root `USER`、`8080` 暴露�?entrypoint，lumira-ui 镜像必须使用 frozen pnpm lockfile、执行生产构建、安装生�?nginx 配置并暴�?`80`。它不能替代真正�?Docker build/inspect 证据，但能在 CI 早期阻止 Dockerfile 发布契约漂移�?

`bin/ddd-release-evidence-manifest-sync.test.mjs` 会校验本清单中所�?`artifacts/ddd/**` 关键 JSON 证据都被共享 manifest contract 覆盖，并禁止�?`release/evidence-manifest.json`、`release/release-evidence-gate.json`、`release/readiness-summary.*` �?orchestrator 报告纳入 checksum 必需清单，避免生成顺序形成循环依赖。新增发布证�?artifact 时必须同步更新清单、manifest contract 和该测试。`bin/ddd-backend-evidence-contract.test.mjs` 会扫�?`services/**/src/test/java/**/*DomainModelsTest.java`、`*ArchitectureBoundaryTest.java` �?`*ArchitectureContractTest.java`，确保每�?bounded-context 领域模型测试�?DDD 架构契约测试都进�?`requiredBackendTestClasses`，新增领�?架构护栏测试时不能绕�?backend release evidence�?

`bin/ddd-rollback-drill-sync.test.mjs` 会校�?`doc/35-ddd-rollback-drill-template.json`、`bin/ddd-init-rollback-drill.mjs` �?rollback deferral owner handoff 都覆盖共�?contract 中的十个 bounded context，避免新增或重命名上下文�?rollback drill 模板、初始化草稿、owner 分派文件�?release gate 漂移�?

`bin/ddd-rollback-drill-evidence.mjs` 会在校验已有 rollback draft 时，�?`DDD_EVIDENCE_ENVIRONMENT`、`DDD_RELEASE_CANDIDATE`、`DDD_EVIDENCE_OPERATOR` 补齐缺失�?artifact metadata；这只消除来源元数据缺口，不会把 `TODO` 上下文改成通过。`DDD_ROLLBACK_DRILL_CHECK_ENV=true` 只生�?`rollback-drill-handoff.md` �?`DDD_ROLLBACK_DRILL_HANDOFF_FILE` 指定的交接文件，不会写回�?rollback artifact，也不会替代真实 PASS/DEFERRED 证据。确实需要批量延期时，可先运�?`bin/ddd-rollback-deferral-template.mjs` 生成覆盖十个 bounded context �?deferral 输入模板，再通过 `DDD_ROLLBACK_DRILL_DEFERRAL_FILE` 提供已审批文件，其中每个上下文都必须带真�?`notExercisableReason`、`riskAcceptedBy`、`deferralEvidence` 和未�?`expiresAt`；脚本会把已应用的上下文写入 `appliedDeferrals`、`contextDiagnostics[].deferralApplied` 和机器校验的 `summary`。共�?contract 会校�?`summary.requiredContexts/contexts/passContexts/deferredContexts/readyContexts/missingRequiredContexts/unknownContexts/duplicateContexts/diagnostics/appliedDeferrals/blockers/warnings` 与明细一致。readiness summary �?rollback action 会输出结构化 `reason`，明确每个上下文是缺 PASS 演练证据还是缺已审批 DEFERRED 风险接受，避�?release priority 中出现无法执行的空原因。`bin/ddd-rollback-deferral-template.test.mjs`、`bin/ddd-rollback-drill-sync.test.mjs` �?`bin/ddd-rollback-drill-evidence.test.mjs` 覆盖该行为，确保真实 PASS/DEFERRED 演练证据仍是 strict 发布门槛�?

`bin/ddd-frontend-smoke-flow-sync.test.mjs` 会校验共�?lumira-ui smoke required-flow 清单�?`lumira-ui/tests/e2e/app.spec.ts` 中的 `@smoke` 测试标题保持同步；新增、重命名或删除核心前端流程时，必须同时更�?contract �?Playwright 测试，否�?`lumira-ui-smoke.json` 转换阶段会把流程判定为缺失�?

`bin/ddd-migration-evidence-sync.test.mjs` 会校验共�?migration contract、`ddd-migration-evidence.mjs`、真�?`services/**/src/main/resources/db/migration*` 目录�?`doc/27-ddd-owner-table-manifest.csv` 保持同步，并要求脚本保留 `DDD_MIGRATION_CHECK_ENV` 预检模式、`DDD_MIGRATION_HANDOFF_FILE` 重定向能力和 `migration-evidence-handoff.md` 输出；新�?owner migration 目录或迁�?owner 表归属时，必须先让该测试通过，避�?Flyway evidence 清单和真实部署目录漂移�?

`bin/ddd-migration-evidence.mjs` 默认会保护已�?fresh DB / old DB upgrade 演练证据：如果目�?`migration-evidence.json` 已经包含 `freshDatabaseValidated`、`upgradeDatabaseValidated` 或对�?Flyway/schema-history 证据引用，普通本地运行不能把这些字段覆盖为空；只有明确设�?`DDD_MIGRATION_ALLOW_EVIDENCE_DOWNGRADE=true` 才能有意替换。`bin/ddd-migration-evidence-downgrade.test.mjs` 会在 CI 中覆盖这个发布证据防降级行为�?

readiness summary 会把迁移演练拆成 `runtimeReady`、fresh DB proof、old DB upgrade proof �?runtime evidence metadata，把回滚演练拆成 release metadata 与各 bounded context drill。不要只�?blocker 总数；优先按 `Actions By Category` 分派 owner�?
