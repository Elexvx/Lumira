# Lumira 报名—评审—获奖—证书全流程用户测试缺陷报告

测试日期：2026-07-30
测试方式：本地隔离数据库、真实前端页面操作、后端日志与数据库交叉核对
测试赛事：`Lumira 全流程验收赛 20260730-2047`
赛事编号：`202607302028266796`

## 修复验收结论

截至 2026-07-31，本报告列出的 DEF-01 至 DEF-10 已全部修复并通过自动化回归；修复过程中发现的前端授奖表单错绑、授奖 SQL 歧义、快照型报名无法授奖、专家审批事件契约不一致及在线迁移兼容性问题也已一并修复。

使用全新隔离数据重新从用户界面完成了完整流程：

1. 创建并发布赛事。
2. 普通用户完成零元报名。
3. 用户自主申请专家，管理员通过工作流审批，事件自动关联原账号并授予专家角色。
4. 创建双盲评审方案、冻结候选、分配专家、评分、汇总、终审并发布结果。
5. 创建并发布证书模板。
6. 从已发布评审结果确认一等奖授奖记录。
7. 从授奖记录生成证书。
8. 获奖用户在“我的证书”查看和下载。
9. 使用证书编号+校验码及二维码 Token 两种方式公开验真。

最终验收数据：

| 对象 | 标识 | 验收状态 |
|---|---|---|
| 赛事 | ID 2 / `E2E 全链路验收赛事 20260730-v2` / `202607302225492619` | 正常创建、发布并立即可见 |
| 报名 | ID 1 / `REG-20260730223316384-NJT` | `CONFIRMED`，零元报名正常跳转成功页 |
| 评审批次 | ID 1 / `RB-20260730230930338-81A1A4F2` | `PUBLISHED` |
| 评审结果 | 候选 `C00001` | 90.2 分 / 第 1 名 / `PASS` |
| 授奖记录 | ID 1 | 一等奖 / `ISSUED` / 已关联证书 ID 1 |
| 证书批次 | ID 1 / `CB-20260730232422454` | `COMPLETED`，成功 1、失败 0 |
| 证书 | ID 1 / `CERT-2026-000001` | `ISSUED`，关联赛事、阶段、报名、用户和授奖记录 |
| 个人证书 | `/certificates/mine` | 正常展示赛事、项目、奖项、日期和下载入口 |
| 公开验真 | 编号+校验码、二维码 Token | 均返回“有效证书”，获奖团队名称按公开策略脱敏 |

### 原缺陷关闭状态

| 缺陷 | 状态 | 关键修复与复验证据 |
|---|---|---|
| DEF-01 双盲身份泄露 | 已关闭 | 盲审快照升级为 schema v2 并递归脱敏；专家评分页实测只显示盲号、项目必要字段和允许采集项，不含团队、负责人、成员、权利人和用户 ID |
| DEF-02 证书日期映射失败 | 已关闭 | 通用行映射器支持 SQL temporal 类型到 `LocalDate`/`LocalDateTime`；证书批次、列表、下载和两种公开验真实测成功 |
| DEF-03 首次审批乐观锁失败 | 已关闭 | 流程终结前重新加载实例状态；专家申请首次审批通过 |
| DEF-04 授奖—证书—领取未闭环 | 已关闭 | 新增授奖实体、表、仓储、接口和生成入口；证书关联发布版本、赛事、阶段、报名和用户；“我的证书”可查看下载 |
| DEF-05 零元报名不跳转 | 已关闭 | 成功状态先导航，再异步清理草稿；真实零元报名正常进入成功页 |
| DEF-06 新赛事列表不可见 | 已关闭 | 修复 SQL temporal 到字符串映射；赛事创建后管理列表立即可见 |
| DEF-07 知识产权默认强制填写 | 已关闭 | 保留完整模板项但默认关闭知识产权采集，只有赛事方显式启用后才出现 |
| DEF-08 结果页仅显示内部 ID | 已关闭 | 联查并展示赛事名、阶段名和 `REG-...` 报名编号；真实结果页已验证 |
| DEF-09 空白模板可发布 | 已关闭 | 服务端校验必要画布元素，新模板使用完整默认画布；真实模板发布成功，缺失元素由单测覆盖拒绝 |
| DEF-10 i18n、重复菜单和表单警告 | 已关闭 | 补齐文案键、运行时菜单去重、修正表单和废弃组件 API；相关测试全绿 |

### 修复中追加发现并关闭的问题

| 编号 | 级别 | 问题 | 解决方案 |
|---|---|---|---|
| ADD-01 | P1 | 授奖页把模板字段绑定到 `awardForm`，把授奖字段绑定到 `templateForm`，导致提交空 JSON | 交换为正确 Form 实例；真实请求从 3 个 `null` 校验错误恢复为成功授奖 |
| ADD-02 | P1 | 授奖 `insert ... select ... on duplicate key update` 中 `status` 未限定，多表查询触发 MySQL 歧义 | 使用 `competition_award_grant.status` 和目标表字段限定；增加源码契约回归 |
| ADD-03 | P1 | 页面报名允许 `team_id/project_id = 0` 并把信息保存在快照，授奖查询却强制内连接实体表，导致“无可授奖结果” | 项目/团队改为左连接，并优先从报名快照读取团队名和项目名；真实快照型报名成功授奖 |
| ADD-04 | P1 | 专家审批生产者事件键为 `EXPERT_APPROVED:aiadc_expert:<id>`，消费者期待旧键，且工作流 ID 位于嵌套属性 | 对齐真实事件键和嵌套载荷；真实 outbox 重放后事件变为 `DELIVERED` |
| ADD-05 | P1 | 已登录申请人审批后可能重复创建账号，且没有自动授予专家角色 | 基于可信 `created_by/created_by_uuid` 复用原账号、绑定专家、授予 `EXPERT` 角色并跳过激活邮件 |
| ADD-06 | P2 | 异步控制面启用时配置属性重复注册，且管理端没有装配 outbox relay 内部作业端点 | 去除重复 Bean 注册并装配内部作业控制器；异步模式管理端启动和事件重放实测成功 |
| ADD-07 | P2 | 通用请求封装不接受合法的 HTTP 204/205 | 将 204/205 视为成功空响应并增加前端单测 |
| ADD-08 | P1 | 在线迁移 V002 假定 `competition_config_item_template` 已存在；从较早版本升级时会直接失败 | 迁移内先幂等建表并补齐默认模板，再关闭知识产权默认采集；已在真实旧版本数据库完成迁移验证 |
| ADD-09 | P1 | 证书模板画布中的 `${recipientName}` 等运行时变量会被 Flyway 当作迁移占位符解析，导致 V003 部署失败 | 为 V003 增加脚本级 `placeholderReplacement=false`，保留证书运行时变量；真实迁移已执行至 V004 |

### 2026-07-31 继续优化

- 授奖来源改为“赛事 → 阶段 → 已发布评审批次”可视化联动选择，不再要求管理员输入内部批次 ID；选项同时展示批次编号和候选人数。
- 授奖规则支持最多 20 档奖项，每档独立配置起止名次；前后端同时拒绝空名称、非法区间和名次重叠。
- 规则重算会取消已不匹配且尚未制证的旧授奖，重新激活当前规则命中的记录；已制证记录及其审计字段保持不可覆盖。
- 重复应用相同规则不重复建档，并明确反馈“未重复建档”；并发制证时区分“记录不存在”“已经制证”和“状态已变化”。
- 授奖列表增加待制证、已制证、已取消汇总，默认只勾选可制证记录；操作按钮支持手机宽度自动换行，表格在窄屏下安全横向滚动。
- 新增自包含的 Playwright 流程用例，覆盖可视化来源选择、三档默认奖项、请求载荷、重复授奖幂等、制证和最终状态；登录链路仍由独立认证 E2E 覆盖。
- 浏览器实测在 1280、768、560 和 390 像素宽度下均无页面横向溢出；390 像素下两个主操作按钮正确换行。

### 自动化与构建结果

- 后端全量：`.\mvnw.cmd test`，22 个模块 `BUILD SUCCESS`；358 个测试报告、3079 个测试，0 失败、0 错误、4 跳过。
- 后端生产包：`.\mvnw.cmd -DskipTests package`，22 个模块 `BUILD SUCCESS`。
- 前端全量：`corepack pnpm test`，69 个测试文件、321 个测试全部通过。
- 前端类型检查：`corepack pnpm run typecheck`，通过。
- 前端生产构建：`corepack pnpm run build`，通过，产物输出到 `lumira-ui/dist`。
- 数据库迁移契约：`node --test bin/database-migration-contract.test.mjs`，16/16 通过。
- 证书模块专项：`CertificateAppServiceTest` 42/42 通过；专家、工作流、事件和内部作业专项测试均已通过。
- 授奖制证 E2E：`certificate-award-workflow.spec.ts` 1/1 通过；浏览器手工复核同时通过。

### 关键实现位置

- 双盲安全边界：`ReviewAppService.java`、`JdbcReviewRepository.java`、`CompetitionReviewPage.tsx`。
- SQL temporal 映射：`BeanPropertyRowMapper.java` 及对应测试。
- 工作流终态并发控制：`WorkflowAppService.java`。
- 授奖与证书闭环：`CertificateAppService.java`、`JdbcCertificateRecordRepository.java`、`CertificateV2Controller.java`、`CertificateManagementPages.tsx`、`MyCertificatesPage.tsx`。
- 数据库演进：`V202607300002__make_intellectual_property_collection_opt_in.sql`、`V202607300003__harden_certificate_generation_and_template_defaults.sql`、`V202607300004__add_competition_award_certificate_closure.sql`。
- 专家申请和事件闭环：`ExpertV2Controller.java`、`ExpertApprovalEventConsumer.java`、`JdbcExpertApprovalRepository.java`、`PlatformEventProperties.java`、`SystemInfrastructureControlPlaneAssemblyConfiguration.java`。
- 前端请求与菜单稳定性：`requestCoreRequest.ts`、`authenticatedMenuTree.ts`、`runtimeMenuDedupe.ts`。

### 安全不变量复核

- 双盲专家端只消费服务端生成的 schema v2 安全快照，原始报名快照仅对有权限的管理端可见。
- 获奖证书的个人查询同时绑定用户 ID 与不可变 UUID；团队成员访问还要求有效团队成员关系。
- 公开验真只返回 `ISSUED`/`REVOKED` 等可公开证书状态，未签发或生成失败记录不对外暴露。
- 公开姓名/团队名经过脱敏；本次有效证书公开显示为 `L*队`。
- 每条发布结果通过唯一约束最多关联一张有效证书；授奖到证书链接更新使用状态条件，避免重复签发。

## 初始结论（修复前基线）

报名、专家评分、汇总、终审和结果发布可以在若干数据库绕行后完成，但“获奖结果自动转证书、获奖者领取、下载与公开查验”的业务闭环未打通。

最终状态：

- 报名记录：`REG-20260730204222980-QUM`，已确认
- 评审结果：90.2 分，第 1 名，终审通过，已发布
- 证书记录：`CERT-2026-000001`，数据库状态 `ISSUED`
- 证书批次：数据库状态 `FAILED`
- 管理端证书结果、证书记录、公开查验：均不可用
- 证书 PNG：已落盘，但页面无法获取或下载

## 关键测试数据

| 对象 | 标识 | 状态 |
|---|---|---|
| 赛事 | ID 1 / `202607302028266796` | 已发布（测试绕行） |
| 报名 | ID 1 / `REG-20260730204222980-QUM` | `CONFIRMED` |
| 评审方案 | ID 1 / `Lumira E2E Final Review` | `READY` |
| 评审批次 | ID 1 / `RB-20260730204818421-1A2A42F8` | `PUBLISHED` |
| 评审结果 | 候选 `C00001` | 90.2 / 第 1 名 / `PASS` |
| 证书 | ID 1 / `CERT-2026-000001` | `ISSUED`，但所属批次 `FAILED` |

## 缺陷清单

### DEF-01 [P1] 双盲评审泄露参赛成员身份

复现：

1. 创建 `DOUBLE_BLIND` 评审方案。
2. 冻结候选、分配专家并开始评分。
3. 打开评分弹窗。

实际：

- 评分弹窗直接展示完整候选快照 JSON。
- 项目知识产权字段中出现 `"rightsHolder": "测试队员小明"`。
- 页面同时声明“匿名评审场景不会返回团队、负责人和成员身份字段”。

预期：

- 双盲快照必须递归移除姓名、权利人、联系方式、用户 ID 等可识别字段。
- 评分页面应以结构化只读字段展示必要材料，而不是输出原始 JSON。

影响：破坏双盲评审公正性，属于隐私与业务完整性问题。

代码证据：

- `ReviewAppService.immutableReviewSnapshot` 将完整 `project_snapshot_json` 放入盲审快照，未递归脱敏。
- `CompetitionReviewPage.tsx` 直接 `JSON.stringify` 展示候选快照。

### DEF-02 [P1] 证书生成后 SQL 日期映射失败，生成、列表、查验和下载全链路不可用

复现：

1. 创建并发布证书模板。
2. 在“证书生成”录入获奖团队、赛事、奖项和日期。
3. 点击“生成单张证书”。
4. 进入“证书记录”或公开查验页。

实际：

- 生成页始终显示“暂无数据”，无成功或失败提示。
- `certificate_record` 已插入且状态为 `ISSUED`。
- `certificate_batch` 同时被标记为 `FAILED`，错误为 `Failed to map SQL row`。
- 管理端证书记录为空；二维码 Token 查验和编号+校验码查验均无结果。
- 服务端异常：

  `java.sql.Date` 无法转换为 `java.time.LocalDate`，属性为 `issueDate`。

预期：

- 证书记录映射成功，批次与记录状态一致。
- 管理端可以查看、下载；公开 Token 和编号查验均可返回证书信息。

影响：证书功能整体阻断，并产生“批次失败、记录已签发”的数据不一致。

代码证据：

- `JdbcCertificateRecordRepository.recordSelect()` 返回 SQL `DATE` 字段。
- 通用 `BeanPropertyRowMapper` 未支持 `java.sql.Date -> LocalDate`。
- 相同映射被列表、按 ID、按 Token、按证书编号查询共同使用。

### DEF-03 [P1] 新建专家的首个审批任务无法通过

复现：

1. 管理员新建专家。
2. 进入 `/workflows/tasks`。
3. 对新产生的 PENDING 任务点击“通过”，填写意见并确认。
4. 重试一次。

实际：

- 弹窗不关闭，任务持续为 `PENDING`。
- 两次服务端均返回：

  `Workflow instance changed, please retry`

预期：无并发修改时首次审批直接成功，专家变为已通过并生成/关联账号。

根因线索：

- `advanceToNext` 先把 `current_node_key` 从 `review` 更新为 `end`。
- 随后调用 `approveInstance(instance, ...)` 时继续使用旧 `instance.currentNodeKey()` 作为乐观锁条件。
- 数据库已是 `end`，旧值仍为 `review`，更新行数为 0，事务回滚。

### DEF-04 [P1] 已发布评审结果没有“授奖—证书—获奖者领取”业务关联

实际：

- 评审终态只有 `PASS`、90.2 分和第 1 名，没有奖项实体或奖项名称。
- 证书生成仅支持手工/CSV，不能从已发布评审结果选择获奖者。
- 手工生成的证书 `competition_id`、`stage_id`、`registration_id`、`user_id` 均为空。
- 普通用户没有证书入口，无法领取或下载属于自己的证书。

预期：

- 已发布结果可以配置/生成奖项（如一等奖）。
- 证书批次可直接选择发布版本和获奖结果。
- 证书应关联赛事、阶段、报名、团队和用户。
- 获奖用户可在个人中心查看、下载和查验。

影响：业务上无法完成真正的“拿奖并领取证书”闭环。

### DEF-05 [P2] 零元报名已经成功，但向导仍停留在“确认并生成订单”

复现：

1. 报名费用设为 CNY 0.00。
2. 完成全部报名信息。
3. 点击“确认并生成订单”。

实际：

- 数据库已生成 `CONFIRMED` 报名，报名列表也能看到。
- 当前页面仍停留在第 5 步，按钮仍为“确认并生成订单”。
- 用户必须手动返回报名列表才能确认是否成功。

预期：零元报名确认后跳转支付结果/报名成功页，明确提示成功。

根因线索：

- 成功分支先 `await clearCompetitionRegistrationDraft(...)`，再执行 `history.push(...)`。
- 草稿清理失败会进入外层 `catch`，成功跳转被中断。

风险：用户可能重复提交，且无法判断报名是否成功。

### DEF-06 [P2] 新建赛事已落库，但赛事管理列表持续“暂无数据”

复现：

1. 管理员通过页面完成赛事创建。
2. 页面提示“赛事已新增”。
3. 返回赛事管理并刷新。

实际：

- 管理列表仍显示“暂无数据”。
- 数据库存在 ID 1 的赛事记录。
- 将赛事在隔离库中置为已发布后，普通用户报名选择器可以看到该赛事。

预期：创建成功后管理列表立即显示赛事，管理员可以继续发布和配置。

影响：阻断正常发布；本次测试只能通过数据库状态更新继续。

### DEF-07 [P2] 创建端未显式配置知识产权，报名端却强制至少填写一项

复现：

1. 创建赛事时不配置初赛材料或知识产权要求。
2. 参赛者进入“项目佐证材料”并直接下一步。

实际：

- 页面默认出现“知识产权 1”。
- 下一步后提示“请至少填写一项知识产权信息”。
- 必须填写类型、名称、权利人和区域才能继续。

预期：仅在赛事方明确启用且标记必填时强制填写；创建端应清晰展示默认采集项。

### DEF-08 [P2] 参赛者结果页只显示内部 ID，缺少业务名称与编号

实际显示：

- `赛事 #1`
- `阶段 #1`
- 报名 `#1`

数据库和其他页面已有赛事名称、阶段名、报名编号。

预期：显示赛事名称、阶段名称和 `REG-...` 报名编号。

### DEF-09 [P2] 证书模板可在几乎空白的状态下直接发布

实际：

- 新模板默认仅包含获奖人、奖项和二维码。
- 未配置背景、标题、赛事、项目、日期时仍可直接发布。
- 生成的 3508×2480 PNG 大面积空白，只有页面中下部姓名/奖项和右下角二维码。

预期：

- 发布前提供完整预览并校验必要元素。
- 至少要求证书标题、获奖人、奖项、赛事、日期和可验证二维码，或明确告警后再发布。

证据文件：`storage/certificates/1/CERT-2026-000001.png`

### DEF-10 [P3] 页面存在国际化缺键、重复菜单键和表单警告

浏览器控制台可重复看到：

- 缺少中文国际化键：
  - `page.login.passwordReset.title`
  - `page.login.passwordReset.next`
  - `page.login.passwordReset.stepAccount`
  - `page.login.passwordReset.stepContact`
  - `page.login.passwordReset.stepPassword`
  - `page.login.passwordReset.account`
  - `global.float.more`
- 菜单重复 Key：
  - `/certificates/templates`
  - `/experts/management`
  - `/expert-review/reviews`
  - `/competitions/register`
- Ant Design `useForm` 未连接警告及多处废弃 API 警告。

影响：增加前端运行噪声，部分语言/菜单场景可能出现错误文案或不稳定渲染。

## 测试绕行说明

为继续覆盖后续链路，本次只在隔离数据库中进行了以下绕行：

1. 赛事管理列表不可见后，将赛事状态改为已发布。
2. 因未能走正常发布动作，补建一个 `ENABLED` 的 Final 阶段。
3. 专家审批接口失败后，将测试专家标记为已审批，并关联管理员账号。

所有评分、汇总、终审、发布、模板设计和证书生成动作仍通过真实页面完成。

## 建议修复顺序

1. 修复 DEF-01 双盲脱敏和 DEF-02 证书日期映射。
2. 修复 DEF-03 工作流乐观锁旧状态问题。
3. 补齐 DEF-04 评审结果、奖项、证书和获奖者的业务关联。
4. 修复 DEF-05、DEF-06，确保主流程成功状态可见。
5. 处理 DEF-07 至 DEF-10 的配置透明度和体验问题。
