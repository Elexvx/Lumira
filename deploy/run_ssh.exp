#!/usr/bin/expect -f
set timeout -1
set password "151505"
set host "192.168.1.102"
set user "root"
set cmd [lindex $argv 0]

spawn ssh -o StrictHostKeyChecking=no $user@$host $cmd
expect {
    "*assword:*" {
        send "$password\r"
        exp_continue
    }
    eof
}
