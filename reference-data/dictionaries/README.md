# 系统字典参考数据

此目录保存不适合内嵌到数据库初始化 SQL 的大体量、版本化系统字典。每个可自动安装的数据包必须包含：

- `manifest.json`：字典代码、结构类型、版本、截止日期、行数、来源和 SHA-256；
- 规范化 `.xlsx` 数据文件；
- `SOURCES.md`：数据来源与转换说明。

服务首次启动时会校验清单和文件哈希，并在数据库锁保护下幂等安装。相同版本不会重复写入；后续数据包版本不会自动覆盖已经投入使用的字典。

`templates/` 提供普通字典和树形字典的 TXT / Excel 导入模板。
