# Local same-origin proxy fallback.
UMI_APP_API_PREFIX=/api

# Production backend origin. When set, requests use `${UMI_APP_API_BASE_URL}/api`
# and WebSocket uses `${UMI_APP_API_BASE_URL}/ws/message`.
UMI_APP_API_BASE_URL=https://api.elexvx.com
UMI_APP_REQUEST_TIMEOUT=10000
