import { Card, Typography } from 'antd';

export default () => (
  <Card>
    <Typography.Title level={4}>首页占位</Typography.Title>
    <Typography.Paragraph>当前为第一轮工程底座初始化页面。</Typography.Paragraph>
  </Card>
);
